Intent: Fix Library.tsx upstream defaults so 'all' mode (mixed audiobook + ebook) is reachable. Three paths blocked it: activeModeLabel defaulted to audiobooks, modeBooksForMedia returned only audiobooks when format unset, empty-state switch-format logic used string comparisons instead of activeFormatTab enum. The fix reduces indirect label comparisons by switching to format-tab enums for conditional logic.
