/ce-orchestrator /Users/pedro/.claude/plans/context-compound-engineering-prompt1-md-inherited-creek.md

Constraints: Branch from main (Stories 1 and 2 must be merged; 2-week measurement window must have passed). Story 3 only — ACs 21-26 from docs/brainstorms/2026-04-18-global-search-upgrade-requirements.md. Prefix parser is strict: position 0 + colon + non-empty content or trailing space. Visible chip UI in the input; Backspace removes chip. Per-page input removal is CONDITIONAL on measurement — if the measurement criteria in §5.4 weren't met, ship prefix filters only and defer removal. Add header "Search" button per page opening palette pre-scoped. Discoverability hint row appears in empty state; dismissal persists in localStorage. No Dexie version bump (uses existing searchFrecency from Story 2). Pick next available E##-S## slot from sprint-status.yaml.
