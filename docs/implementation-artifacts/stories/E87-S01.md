---
story_id: E87-S01
story_name: "Audiobook Import and Data Model (Dexie v31)"
status: ready-for-dev
started:
completed:
reviewed: false
review_started:
review_gates_passed: []
burn_in_validated: false
---

# Story 87.1: Audiobook Import and Data Model (Dexie v31)

Status: ready-for-dev

## Story

As a learner,
I want to import an audiobook as a folder of MP3 files,
so that my personal audiobook collection is available in Knowlune alongside my EPUBs.

## Acceptance Criteria

**Given** the Dexie schema is updated (v31 bump adding `audioBookmarks` table with indexes on `bookId`, `chapterIndex`, `timestamp`, `createdAt`)
**When** the user clicks "Import Book" on the Library page
**Then** the import dialog now also accepts "Audiobook (MP3 folder)" as an import type
**And** the user can select multiple MP3 files (one per chapter)

**Given** the user selects a folder of MP3 files
**When** the import processes
**Then** filenames are parsed for chapter order (e.g., `01-introduction.mp3`, `02-chapter-one.mp3`)
**And** metadata (title, author) is extracted from the first MP3 file's ID3 tags if available
**And** all MP3 files are stored in OPFS at `/knowlune/books/{bookId}/chapter-{nn}.mp3`
**And** a Book record is created with `format: 'audiobook'`, chapters array with `startTime` offsets, and `totalDuration` calculated from all files
**And** the book appears in the Library with an audiobook badge/indicator

## Tasks / Subtasks

- [ ] Task 1: Dexie v31 schema migration (AC: 1)
  - [ ] 1.1 Bump `CHECKPOINT_VERSION` to 31 in `src/db/checkpoint.ts`
  - [ ] 1.2 Add `audioBookmarks` table to schema: `'id, bookId, chapterIndex, timestamp, createdAt'`
  - [ ] 1.3 Add `AudioBookmark` type to `src/data/types.ts` (if not already from E83): `{ id, bookId, chapterIndex, timestamp, note?, createdAt }`
  - [ ] 1.4 Test migration: existing data survives version bump

- [ ] Task 2: MP3 file selection and chapter parsing (AC: 2, 3)
  - [ ] 2.1 Extend `BookImportDialog` (from E83) to add "Audiobook (MP3 folder)" import type
  - [ ] 2.2 Use `<input type="file" multiple accept=".mp3">` for file selection
  - [ ] 2.3 Parse filenames for chapter ordering: extract leading numbers, sort numerically, derive chapter titles from filename remainder
  - [ ] 2.4 Handle edge cases: no leading numbers (sort alphabetically), mixed naming, single-file audiobooks

- [ ] Task 3: MP3 metadata extraction (AC: 3)
  - [ ] 3.1 Read ID3 tags from first MP3 file using browser AudioContext or lightweight ID3 parser
  - [ ] 3.2 Extract: title (ID3 album or title tag), author (ID3 artist tag), cover art (ID3 APIC frame if present)
  - [ ] 3.3 Pre-populate import dialog fields with extracted metadata
  - [ ] 3.4 Fallback: if no ID3 tags, use folder/file name heuristics for title

- [ ] Task 4: OPFS storage for audio files (AC: 3)
  - [ ] 4.1 Store each MP3 file via `OpfsStorageService` at `/knowlune/books/{bookId}/chapter-{nn}.mp3`
  - [ ] 4.2 Store cover art (if extracted) at `/knowlune/books/{bookId}/cover.jpg`
  - [ ] 4.3 Show import progress: "Storing chapter {n} of {total}..."

- [ ] Task 5: Create Book record for audiobook (AC: 3, 4)
  - [ ] 5.1 Calculate `totalDuration` by summing duration of all MP3 files (use `Audio` element `loadedmetadata` event for each file)
  - [ ] 5.2 Build `chapters` array: `{ id, title, startTime, order }` where `startTime` is cumulative offset
  - [ ] 5.3 Create Book record with `format: 'audiobook'`, `source: { type: 'local', opfsPath }`, chapters, and totalDuration
  - [ ] 5.4 Emit `book:imported` event on event bus

- [ ] Task 6: Library audiobook badge (AC: 4)
  - [ ] 6.1 In BookCard and BookListItem (from E83), show an audiobook indicator (Headphones icon from lucide-react) when `book.format === 'audiobook'`
  - [ ] 6.2 Use `bg-muted text-muted-foreground` badge styling

- [ ] Task 7: Tests (AC: 1, 2, 3, 4)
  - [ ] 7.1 Test Dexie v31 migration adds `audioBookmarks` table
  - [ ] 7.2 Test filename parsing: numbered files sort correctly, edge cases handled
  - [ ] 7.3 Test Book record created with correct format, chapters, and duration
  - [ ] 7.4 Test library shows audiobook badge

## Dev Notes

### Architecture Patterns

- **Dexie checkpoint pattern**: `src/db/checkpoint.ts` manages schema versions. Bump from v30 (added by E83) to v31. Follow exact same pattern as existing version bumps.
- **Schema file**: `src/db/schema.ts` — defines the Dexie database. Add `audioBookmarks` table here.
- **OPFS storage**: `src/services/OpfsStorageService.ts` (created in E83) — reuse for MP3 file storage.
- **BookImportDialog**: `src/app/components/library/BookImportDialog.tsx` (created in E83) — extend, don't replace.
- **Event bus**: `src/lib/eventBus.ts` — emit `book:imported` event.

### ID3 Tag Extraction

For lightweight ID3 parsing without adding a heavy dependency (music-metadata is deferred to E88 for M4B):
- Option A: Use the browser `Audio` element to get duration, and a lightweight ID3v2 parser (< 5KB) for title/artist/cover
- Option B: Parse raw ID3v2 header bytes manually (ID3v2 spec is well-documented, headers are at file start)
- Do NOT import `music-metadata` here — that's a ~200KB dependency reserved for M4B in E88

### Chapter Duration Calculation

```typescript
// Get duration of each MP3 using HTML5 Audio
async function getAudioDuration(file: File): Promise<number> {
  return new Promise((resolve) => {
    const audio = new Audio()
    audio.addEventListener('loadedmetadata', () => resolve(audio.duration))
    audio.src = URL.createObjectURL(file)
  })
}
```

### Dependencies

- **Hard dependency on E83**: Book data model, BookImportDialog, OpfsStorageService, useBookStore, Library page
- **No dependency on E84/E85**: Audiobook import is independent of EPUB reader

### Key Files to Touch

- `src/db/checkpoint.ts` — bump to v31
- `src/db/schema.ts` — add audioBookmarks table
- `src/data/types.ts` — add AudioBookmark type (if not already present)
- `src/app/components/library/BookImportDialog.tsx` — extend with audiobook import
- `src/app/components/library/BookCard.tsx` — add audiobook badge
- `src/app/components/library/BookListItem.tsx` — add audiobook badge

### References

- [Source: _bmad-output/planning-artifacts/epics-books-audiobooks-library.md — E87-S01]
- [Source: _bmad-output/planning-artifacts/architecture-books-audiobooks-library.md — Decision 1 (Dexie schema), Decision 2 (OPFS)]
- [Source: src/db/checkpoint.ts — existing checkpoint pattern]

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
