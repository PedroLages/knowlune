---
story_id: E63-S03
story_name: "Prompt Builder Slot 5 Integration"
status: ready-for-dev
started:
completed:
reviewed: false
review_started:
review_gates_passed: []
burn_in_validated: false
---

# Story 63.3: Prompt Builder Slot 5 Integration

## Story

As a learner using the AI tutor,
I want the tutor to understand my learning strengths and weaknesses when answering my questions,
So that I receive personalized guidance that addresses my specific struggles rather than generic responses.

## Acceptance Criteria

**Given** the `tutorPromptBuilder` assembles a system prompt for a 4K Ollama model
**When** the LEARNER_PROFILE slot (slot 5) is populated via `buildLearnerProfile`
**Then** the slot content fits within 40 tokens
**And** the total system prompt does not exceed the 4K token budget

**Given** the `tutorPromptBuilder` assembles a system prompt for a 128K model
**When** the LEARNER_PROFILE slot is populated
**Then** the slot content uses up to 100 tokens of richer profile data

**Given** E52 quiz system is not yet implemented and `db.quizAttempts` is empty
**When** `buildLearnerProfile` is called during prompt assembly
**Then** no error is thrown
**And** the quiz section is simply omitted from the profile
**And** other available signals (flashcards, study sessions) still appear

**Given** none of the upstream epics (E52, E56, E59) are implemented and all data sources are empty
**When** `buildLearnerProfile` is called during prompt assembly
**Then** it returns an empty string
**And** the prompt builder omits slot 5 entirely (existing behavior for empty optional slots)

**Given** the tutor prompt builder calls `buildLearnerProfile` asynchronously
**When** the profile is built
**Then** the builder properly awaits the Promise before assembling the final system prompt

**Given** course tags are available from `courseTagger.ts` for the current course
**When** the prompt builder calls `buildLearnerProfile`
**Then** it passes course tags as `lessonTopics` for topic-scoped profile filtering

## Tasks / Subtasks

- [ ] Task 1: Understand existing slot 5 implementation (AC: all)
  - [ ] 1.1 Read `src/ai/tutor/tutorPromptBuilder.ts` (created by E57) to understand the 6-slot architecture
  - [ ] 1.2 Identify how slot 5 (LEARNER_PROFILE) currently populates its content
  - [ ] 1.3 Identify the `maxTokens` allocation for slot 5 per model tier (40 for 4K, 100 for 128K)
  - [ ] 1.4 Understand how the builder handles empty optional slots (should already omit them)

- [ ] Task 2: Wire `buildLearnerProfile` into slot 5 (AC: 1, 2, 5)
  - [ ] 2.1 Import `buildLearnerProfile` from `./learnerProfileBuilder`
  - [ ] 2.2 Replace current slot 5 content generation with `await buildLearnerProfile({ courseId, maxTokens: slotBudget })`
  - [ ] 2.3 Ensure the prompt builder awaits the async call (buildLearnerProfile returns a Promise)
  - [ ] 2.4 Preserve the basic progress data (progress %, lesson position, streak) as a fallback when profile returns empty string

- [ ] Task 3: Pass lesson topics from course tagger (AC: 6)
  - [ ] 3.1 Import `courseTagger` from `src/ai/courseTagger.ts`
  - [ ] 3.2 Resolve course tags for the current course
  - [ ] 3.3 Pass tags as `lessonTopics` in the `ProfileBuilderConfig`

- [ ] Task 4: Graceful degradation verification (AC: 3, 4)
  - [ ] 4.1 Verify that when `buildLearnerProfile` returns empty string, slot 5 is omitted (existing behavior)
  - [ ] 4.2 Verify that partial data (e.g., only study sessions available) still produces useful output
  - [ ] 4.3 Ensure no thrown errors bubble up to the prompt builder — all failures handled internally by `buildLearnerProfile`

## Dev Notes

### Architecture: E57 Slot-Based Prompt Builder
The `tutorPromptBuilder.ts` (from E57) uses a 6-slot priority system:
- Slots filled in priority order (highest first)
- Slot 5 = LEARNER_PROFILE (optional — auto-omitted under 4K token pressure)
- Token budget per slot: 40 tokens (4K Ollama) / 100 tokens (128K models)
- Builder accepts content per slot and truncates to fit

### Integration Points

**File to modify:** `src/ai/tutor/tutorPromptBuilder.ts`
- This file is created by E57 (hard dependency) — must exist before this story
- Modify the slot 5 content provider to call `buildLearnerProfile()`
- The call is async — ensure the prompt builder handles this

**File to import from:** `src/ai/tutor/learnerProfileBuilder.ts` (created in S01/S02)
- `buildLearnerProfile(config: ProfileBuilderConfig): Promise<string>`

**File to import from:** `src/ai/courseTagger.ts`
- Provides course tags that map to lesson topics for topic-scoped filtering

### Fallback Strategy
When `buildLearnerProfile` returns empty string (no data from any source):
- Slot 5 should still include minimal progress data: "Progress: 45%, Lesson 5/12, Streak: 7 days"
- This preserves the existing behavior from before E63
- Only when the enriched profile returns non-empty content does it replace/augment the basic progress line

### Critical: Async Handling
`buildLearnerProfile` returns a Promise (because aggregation functions query Dexie). The prompt builder must `await` this call. If `tutorPromptBuilder.ts` currently generates slot content synchronously, this story must refactor the slot 5 provider to support async content generation.

### Dependencies
- Hard: E57 (AI Tutoring Phase 1-2) — provides `tutorPromptBuilder.ts`
- Hard: E63-S01 and E63-S02 — provides `buildLearnerProfile()`
- Soft: E52, E56, E59 — data sources (all handled via graceful degradation)

### Project Structure Notes
- Modifies: `src/ai/tutor/tutorPromptBuilder.ts` (E57 file)
- Imports from: `src/ai/tutor/learnerProfileBuilder.ts` (E63-S01/S02)
- Imports from: `src/ai/courseTagger.ts` (existing)
- No new files, no new dependencies

### References
- [Source: _bmad-output/implementation-artifacts/tech-spec-ai-tutor-learner-profile-context-injection.md — Task 8]
- [Source: _bmad-output/planning-artifacts/epics-ai-tutor-learner-profile.md — Story 63.3]
- [Source: _bmad-output/planning-artifacts/architecture.md — AI Tutoring slot priority system]
- [Source: src/ai/courseTagger.ts — Course tag generation]

## Testing Notes

Unit tests deferred to E63-S04. Key integration scenarios:
- Slot 5 content within 40-token budget for 4K models
- Slot 5 content within 100-token budget for 128K models
- Empty profile → fallback to basic progress data
- All sources empty → slot 5 omitted entirely
- Async Promise properly awaited before prompt assembly

## Pre-Review Checklist

Before requesting `/review-story`, verify:

- [ ] All changes committed (`git status` clean)
- [ ] No error swallowing — catch blocks log AND surface errors
- [ ] useEffect hooks have cleanup functions (ignore flags for async, event listener removal)
- [ ] No optimistic UI updates before persistence — state updates after DB write succeeds
- [ ] Type guards on all dynamic lookups (e.g., `LABELS[type]` when type can be empty)
- [ ] E2E afterEach cleanup uses `await` (not fire-and-forget)
- [ ] Date handling uses `toLocaleDateString('sv-SE')` pattern (not `toISOString().split('T')[0]`)
- [ ] Read [engineering-patterns.md](../engineering-patterns.md) for full patterns reference
- [ ] If story calls external APIs: CSP allowlist configured (see engineering-patterns.md § CSP Configuration)

## Design Review Feedback

[Populated by /review-story — Playwright MCP findings]

## Code Review Feedback

[Populated by /review-story — adversarial code review findings]

## Challenges and Lessons Learned

[Document issues, solutions, and patterns worth remembering]
