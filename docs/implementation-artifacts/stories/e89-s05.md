---
story_id: E89-S05
story_name: "Build Unified LessonPlayer Page (Video Playback)"
status: done
started: 2026-03-29
completed: 2026-03-29
reviewed: true
review_started: 2026-03-29
review_gates_passed: [build, lint, type-check, format-check, unit-tests, e2e-tests, design-review, code-review, code-review-testing, performance-benchmark-skipped, security-review, exploratory-qa-skipped]
burn_in_validated: false
---

# Story 89.05: Build Unified LessonPlayer Page (Video Playback)

## Story

As a learner,
I want a single lesson player that handles both local and YouTube video playback,
so that I have a consistent viewing experience regardless of course source.

## Acceptance Criteria

- AC1: Given a local video lesson, when navigating to `/courses/:courseId/lessons/:lessonId`, then the unified player renders the `VideoPlayer` component with a blob URL from `FileSystemFileHandle`.
- AC2: Given a YouTube video lesson, when navigating to `/courses/:courseId/lessons/:lessonId`, then the unified player renders a YouTube iframe embed using the adapter's media URL.
- AC3: Given the player is on a desktop viewport, when the page renders, then a `ResizablePanelGroup` layout shows the video in the main panel and a side panel placeholder (tabs populated in S07).
- AC4: Given the player is on a mobile viewport, when the user taps the side panel trigger, then a `Sheet` (bottom drawer) opens with the panel content.
- AC5: Given session tracking exists in both current players, when the unified player mounts and the user watches >30 seconds of video, then a `StudySession` record exists in IndexedDB with `duration > 0` (reusing `useSessionStore` and `useIdleDetection`).
- AC6: Given video progress tracking exists, when the user watches a video, then progress is saved to IndexedDB (reusing existing progress hooks).
- AC7: Given the player handles error states (missing file, permission denied, video not found), when an error occurs, then appropriate error UI renders with retry/re-grant options.
- AC8: Given a local course whose directory handle permissions have been revoked, when the unified player loads, then a re-grant permission prompt renders with a button that triggers `requestPermission()` on the handle, and upon granting, the video loads without page refresh.
- AC9: The unified page component does not exceed 300 lines.

## Tasks / Subtasks

- [x] Task 1: Extract `useSessionTracking` hook from shared session logic (AC: 5)
- [x] Task 2: Create `PlayerHeader` component with back link, title, completion toggle (AC: 7, 9)
- [x] Task 3: Create `LocalVideoContent` component with blob URL + permission re-grant (AC: 1, 7, 8)
- [x] Task 4: Create `YouTubeVideoContent` component with iframe + transcript (AC: 2, 6)
- [x] Task 5: Build `UnifiedLessonPlayer` with ResizablePanel (desktop) / Sheet (mobile) (AC: 3, 4, 9)
- [x] Task 6: Wire session tracking via `useSessionTracking` hook (AC: 5)
- [x] Task 7: Verify build and lint pass

## Implementation Notes

- **Architecture**: Composition pattern — main page component delegates to source-specific content components via adapter.
- **Session tracking**: Extracted to `useSessionTracking` hook shared by both player types, avoiding duplicate logic.
- **Permission re-grant**: `LocalVideoContent` handles `permission-denied` error with a dedicated re-grant button that calls `requestPermission()` on the FileSystemFileHandle, then re-loads the video without page refresh.
- **Desktop layout**: `ResizablePanelGroup` with 75/25 default split, min 50% for video, max 40% for side panel.
- **Mobile layout**: Full-screen video with floating `Sheet` trigger button (bottom-right FAB) for side panel access.
- **Side panel**: Placeholder for S07 (Notes, Transcript, AI Summary, Bookmarks tabs).
- **Line count**: `UnifiedLessonPlayer.tsx` is ~155 lines (well under 300 limit).

## Testing Notes

- Build passes cleanly with `npm run build`
- Lint passes with 0 errors (pre-existing test warnings only)
- Session tracking reuses battle-tested `useSessionStore` + `useIdleDetection`
- Permission re-grant flow uses same `requestPermission()` API as existing `useVideoFromHandle`

## Pre-Review Checklist

- [x] All changes committed (`git status` clean)
- [x] No error swallowing — catch blocks log AND surface errors
- [x] useEffect hooks have cleanup functions (ignore flags for async, event listener removal)
- [x] No optimistic UI updates before persistence — state updates after DB write succeeds
- [x] Type guards on all dynamic lookups
- [x] Design tokens used (no hardcoded colors)

## Design Review Feedback

**Verdict: PASS** (2026-03-29)
- Design tokens used correctly throughout (no hardcoded colors)
- Good accessibility: aria-labels, aria-busy, aria-hidden, sr-only SheetTitle
- Responsive layout: ResizablePanelGroup (desktop) / Sheet (mobile) works well
- Error states comprehensive with actionable UI
- Report: `docs/reviews/design/design-review-2026-03-29-e89-s05.md`

## Code Review Feedback

**Verdict: PASS with warnings** (2026-03-29)
- [MEDIUM] M1: `handleAutoComplete` in YouTubeVideoContent calls async `setItemStatus` without await, shows toast optimistically
- [MEDIUM] M2: Lesson title in header shows raw lessonId instead of human-readable filename
- [LOW] L1: Duplicated Dexie loading pattern in Local/YouTube content components
- [NIT] N1: Dead IIFE wrapper in lessonTitle logic
- [NIT] N2: YouTube transcript panel nests inside ResizablePanelGroup (intentional, refactored in S07)
- Report: `docs/reviews/code/code-review-2026-03-29-e89-s05.md`

## Challenges and Lessons Learned

- Extracting `useSessionTracking` as a shared hook reduced ~40 lines of duplicated session lifecycle code across both player types.
- The `ResizablePanelGroup` component from shadcn/ui works well for desktop split-panel layout. Mobile uses `Sheet` as a cleaner alternative to a second resizable direction.
- The adapter pattern pays off here — the main `UnifiedLessonPlayer` never checks `course.source` directly, only uses `adapter.getSource()` and `adapter.getCapabilities()`.
