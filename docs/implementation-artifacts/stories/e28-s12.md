---
story_id: E28-S12
story_name: "Offline Support, Metadata Refresh & Security Hardening"
status: in-progress
started: 2026-03-26
completed:
reviewed: false
review_started:
review_gates_passed: []
burn_in_validated: false
---

# Story 23.12: Offline Support, Metadata Refresh & Security Hardening

## Story

As a learner,
I want my YouTube course data available offline and my metadata kept fresh per YouTube's terms,
so that I can study notes and transcripts without internet and remain compliant with YouTube API Terms of Service.

## Acceptance Criteria

1. Offline: course detail page shows full chapter structure with progress bars, cached transcript text available for reading/searching, notes/progress/bookmarks/streaks/flashcards work normally
2. Offline: video player area shows "Connect to the internet to watch" with WifiOff icon (UX-DR6)
3. Offline: "Refresh metadata" button disabled with tooltip "Requires internet connection"
4. Metadata refresh: when `lastRefreshedAt` > 30 days old, app startup queues background refresh (non-blocking, rate-limited)
5. Refreshed metadata updates in `youtubeVideoCache` and `importedVideos` (title, duration, thumbnail changes)
6. Removed videos: marked with "removed from YouTube" badge, user's progress/notes/transcript preserved
7. CSP headers: `frame-src` allows youtube.com + youtube-nocookie.com, `img-src` allows i.ytimg.com + img.youtube.com, `connect-src` allows googleapis.com/youtube/
8. AI summaries (Premium): generate from transcript data (FR123), same pattern as local video summaries (FR48), displayed in collapsible panels, uses only transcript text as input (NFR54)
9. Data locality (NFR74): all YouTube data stored entirely in IndexedDB, no transmission except to YouTube API, user's AI provider, and user's Whisper endpoint

## Tasks / Subtasks

- [x] Task 1: Add `lastRefreshedAt` and `removedFromYouTube` fields to types and schema (AC: 4,5,6)
- [x] Task 2: Create `youtubeMetadataRefresh.ts` — background refresh service (AC: 4,5,6)
- [x] Task 3: Update `YouTubeCourseDetail.tsx` — offline support, refresh button, removed badge, AI summary (AC: 1,2,3,6,8)
- [x] Task 4: Update `YouTubeLessonPlayer.tsx` — already has offline placeholder (AC: 2)
- [x] Task 5: Wire `refreshStaleMetadata()` into app startup (AC: 4)
- [x] Task 6: Verify CSP headers in vite.config.ts (AC: 7) — already done in S09
- [x] Task 7: Add unit tests for metadata refresh logic (AC: 4,5,6)

## Implementation Notes

- `useOnlineStatus` hook already exists at `src/app/hooks/useOnlineStatus.ts`
- `YouTubeLessonPlayer` already has offline placeholder (E28-S09)
- CSP headers already configured in `vite.config.ts` from E28-S09
- `lastRefreshedAt` added to `ImportedCourse` type for tracking metadata staleness
- `removedFromYouTube` flag added to `ImportedVideo` for removed video detection

## Testing Notes

- Unit tests cover: stale detection, metadata refresh, removed video detection, rate limiting
