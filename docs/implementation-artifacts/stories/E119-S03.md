---
story_id: E119-S03
story_name: "Registry-Driven Erasure Cascade"
status: ready-for-dev
epic: E119
dependencies: []
plan: docs/plans/2026-04-22-003-feat-e119-gdpr-full-compliance-plan.md
---

# Story E119-S03: Registry-Driven Erasure Cascade

## Story

As a user (data subject),
I want a deletion request to cascade across all my data — all 26 sync tables and 4 Storage buckets — driven by `tableRegistry.ts`,
so that my data is completely erased and the erasure can be verified.

## Acceptance Criteria

- **AC-1**: `delete-account` Edge Function hard-delete phase iterates all `tableRegistry` entries and deletes rows matching `user_id` for each table.
- **AC-2**: Hard-delete phase also iterates `STORAGE_BUCKETS = ['avatars', 'course-media', 'audio', 'exports']` and removes user-prefixed objects.
- **AC-3**: Soft-delete phase (immediate): `deleted_at` set on auth + `pending_deletion_at` marker on shared row; user can still log in to cancel during 7-day grace.
- **AC-4**: `cancel-account-deletion` Edge Function reverses soft-delete; reactivates auth, no data lost.
- **AC-5**: `retention-tick` skeleton function exists (to be fleshed out in S11); includes a branch that calls hard-delete cascade for users whose soft-delete is > 7 days old.
- **AC-6**: Lawful-basis exceptions documented in code comments + linked to `docs/compliance/retention.md` (placeholder for S10): billing rows anonymised/retained, breach-register references pseudonymised.
- **AC-7**: Stripe anonymisation on delete: scrub email/name/address via Stripe API, retain customer + invoices for tax retention.
- **AC-8**: Post-delete probe test asserts every registry table returns zero rows for the test user after hard-delete.
- **AC-9**: Registry-drift CI test: adding a new `tableRegistry` entry without updating cascade code fails the probe test.
- **AC-10**: `supabase/functions/main/index.ts` routes `cancel-account-deletion` and updated `delete-account`.

## Tasks / Subtasks

- [ ] Task 1: Write characterization/probe tests first — query every registry table by user_id, expect zero rows post-delete (AC: 8)
- [ ] Task 2: Extend `supabase/functions/delete-account/index.ts` with registry-driven cascade (AC: 1, 2)
- [ ] Task 3: Implement soft-delete phase with `pending_deletion_at` marker; verify migration if `deleted_at` column missing (AC: 3)
- [ ] Task 4: Create `supabase/functions/cancel-account-deletion/index.ts` (AC: 4)
- [ ] Task 5: Create `supabase/functions/retention-tick/index.ts` skeleton with grace-period hard-delete branch (AC: 5)
- [ ] Task 6: Add Stripe anonymisation path; handle Stripe API failure gracefully (AC: 7)
- [ ] Task 7: Document lawful-basis exceptions in code + link to retention.md placeholder (AC: 6)
- [ ] Task 8: Update `supabase/functions/main/index.ts` router (AC: 10)
- [ ] Task 9: Extend `src/lib/__tests__/deleteAccount.test.ts` with new paths (AC: 8, 9)

## Context

- Existing `delete-account/index.ts` has CORS + JWT auth — extend, don't rewrite.
- `tableRegistry.ts` at `src/lib/sync/tableRegistry.ts` (678 lines, 26 tables) is the backbone. Mirror iteration from `syncEngine.ts`.
- `SOFT_DELETE_GRACE_DAYS = 7` already in `deleteAccount.ts` — preserve this constant.
- S04 (deletion verification email) depends on this story.
- S05 (export) shares the registry iteration pattern from this story.
- S10 (retention matrix) will add the formal `retention.md`; S03 only needs a comment placeholder.
- Characterization-first: write post-delete probe tests BEFORE touching cascade code.

## Out of Scope

- Sending confirmation emails (S04).
- Full retention-tick scheduling (S11).
- Export functionality (S05, S06).
