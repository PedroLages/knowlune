---
story_id: E119-S08
story_name: "Consent UI + Withdrawal Effects"
status: ready-for-dev
epic: E119
dependencies: [E119-S07]
plan: docs/plans/2026-04-22-003-feat-e119-gdpr-full-compliance-plan.md
---

# Story E119-S08: Consent UI + Withdrawal Effects

## Story

As a user (data subject),
I want per-purpose consent toggles in Settings → Privacy with plain-language copy,
so that I can grant or withdraw consent at any time and see exactly what effect that has on my data.

## Acceptance Criteria

- **AC-1**: `src/app/components/settings/sections/PrivacySection.tsx` renders per-purpose toggles from the consent inventory.
- **AC-2**: Each toggle shows: purpose name, data categories affected, "what happens if you withdraw" copy.
- **AC-3**: Withdrawal requires a confirmation dialog listing exactly what will be deleted/frozen before executing.
- **AC-4**: Withdrawal effects are applied atomically (or rolled back on failure — never leave consent=off with effects=incomplete).
- **AC-5**: Effects per purpose:
  - `ai_tutor`: cancel in-flight AI requests; no data deletion.
  - `ai_embeddings`: delete embedding rows; mark learner model snapshot `frozen_reason: 'consent_withdrawn'`.
  - `voice_transcription`: delete pending audio jobs; retain past transcripts.
  - `analytics_telemetry`: delete telemetry records.
  - `marketing_email`: unsubscribe.
- **AC-6**: Toggles default to off for non-core purposes; no pre-ticked; no bundled consents (GDPR Art 7(2)).
- **AC-7**: All AI calls are wrapped with `consentService.isGranted` guard; consent=off → request rejected with user-visible message.
- **AC-8**: Consent row synced to Dexie + Supabase; other devices reflect the change on next sync.
- **AC-9**: Toggles are keyboard-navigable with ARIA labels describing purpose and current state (WCAG 2.1 AA).
- **AC-10**: E2E: grant → use AI → withdraw → effects applied.

## Tasks / Subtasks

- [ ] Task 1: Create `src/app/components/settings/sections/PrivacySection.tsx` with toggle layout (AC: 1, 2, 6)
- [ ] Task 2: Create `src/app/components/settings/ConsentToggles.tsx` component (AC: 1, 2, 9)
- [ ] Task 3: Add Privacy link/tab to `src/app/components/settings/sections/AccountSection.tsx` (AC: 1)
- [ ] Task 4: Locate AI call entry points in `src/lib/ai/*`; wrap with consent guard (AC: 7)
- [ ] Task 5: Create `src/lib/compliance/consentEffects.ts` with effect handlers + rollback logic (AC: 4, 5)
- [ ] Task 6: Wire confirmation dialog using `src/app/components/ui/dialog.tsx` (AC: 3)
- [ ] Task 7: Write unit tests for `ConsentToggles` and `consentEffects` (AC: 4, 5)
- [ ] Task 8: Write E2E `tests/e2e/compliance/consent-withdrawal.spec.ts` (AC: 10)

## Context

- Depends on S07 for consent schema and `consentService`.
- S09 (AI provider re-consent) depends on this story's consent guard pattern.
- Use existing `Switch` component from `src/app/components/ui/`.
- Section pattern from `src/app/components/settings/sections/`.
- No dark patterns: no fake-disabled toggles, no pre-bundled consents, no nudge copy.
- Re-grant: embeddings are NOT auto-regenerated; learner model un-freezes on next write.

## Out of Scope

- AI provider change re-consent (S09).
- Retention enforcement (S11).
