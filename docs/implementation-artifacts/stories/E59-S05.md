---
story_id: E59-S05
story_name: "Retention Metrics and Consumer Updates"
status: in-progress
started: 2026-03-30
completed:
reviewed: false
review_started:
review_gates_passed: []
burn_in_validated: false
---

# Story 59.05: Retention Metrics and Consumer Updates

## Story

As a learner,
I want the retention metrics tests and consumer test code updated to use FSRS field names,
so that all test factories and assertions are consistent with the new FSRS-based ReviewRecord type.

## Acceptance Criteria

**Given** the retentionMetrics test file uses SM-2 fields (`reviewedAt`, `nextReviewAt`, `interval`, `easeFactor`, `reviewCount`)
**When** this story is implemented
**Then** the `makeReview` factory uses FSRS fields (`stability`, `difficulty`, `reps`, `lapses`, `state`, `elapsed_days`, `scheduled_days`, `due`, `last_review`)
**And** all test assertions reflect FSRS retention logic (predictRetention uses stability + last_review)
**And** all retentionMetrics tests pass

**Given** the interleave test file uses SM-2 ReviewRecord fields
**When** this story is implemented
**Then** the `makeRecord` factory uses FSRS fields
**And** all interleave tests pass

**Given** the exportService test uses `reviewedAt` for review records
**When** this story is implemented
**Then** the test uses `last_review` instead of `reviewedAt`
**And** all exportService tests pass

**Given** the NotificationService test uses `nextReviewAt` for due checking
**When** this story is implemented
**Then** the test uses `due` field instead of `nextReviewAt`
**And** all NotificationService tests pass

**Given** all test files are updated
**When** `npm run build` and `npx tsc --noEmit` are run
**Then** zero TypeScript errors in updated test files
**And** all unit tests pass

## Tasks / Subtasks

- [ ] Task 1: Update `retentionMetrics.test.ts` — rewrite `makeReview` factory with FSRS fields, update all test data (AC: 1)
- [ ] Task 2: Update `interleave.test.ts` — rewrite `makeRecord` factory with FSRS fields (AC: 2)
- [ ] Task 3: Update `exportService.test.ts` — change `reviewedAt` to `last_review` in mock data (AC: 3)
- [ ] Task 4: Update `NotificationService.test.ts` — change `nextReviewAt` to `due` in mock data (AC: 4)
- [ ] Task 5: Verify build and type-check pass, run all unit tests (AC: 5)

## Implementation Notes

- The production code (`retentionMetrics.ts`, `interleave.ts`, `exportService.ts`, `NotificationService.ts`) is already migrated to FSRS fields from E59-S02 and E59-S04
- Only test files need updating — test factories still reference old SM-2 field names
- FSRS retention prediction uses `stability` + `last_review` (elapsed days) via power-law forgetting curve
- FSRS due checking uses `due` field (ISO 8601 string) compared to current time

## Testing Notes

[Test strategy, edge cases discovered, coverage notes]

## Pre-Review Checklist

Before requesting `/review-story`, verify:

- [ ] All changes committed (`git status` clean)
- [ ] No error swallowing — catch blocks log AND surface errors
- [ ] useEffect hooks have cleanup functions (ignore flags for async, event listener removal)
- [ ] No optimistic UI updates before persistence — state updates after DB write succeeds
- [ ] Type guards on all dynamic lookups (e.g., `LABELS[type]` when type can be empty)
- [ ] E2E afterEach cleanup uses `await` (not fire-and-forget)
- [ ] Date handling uses `toLocaleDateString('sv-SE')` pattern (not `toISOString().split('T')[0]`)
- [ ] Read [engineering-patterns.md](../engineering-patterns.md) for full patterns reference
- [ ] If story calls external APIs: CSP allowlist configured (see engineering-patterns.md § CSP Configuration)

## Design Review Feedback

[Populated by /review-story — Playwright MCP findings]

## Code Review Feedback

[Populated by /review-story — adversarial code review findings]

## Challenges and Lessons Learned

[Document issues, solutions, and patterns worth remembering]
