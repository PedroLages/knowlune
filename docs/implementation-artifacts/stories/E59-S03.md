---
story_id: E59-S03
story_name: "Dexie v29 Schema Migration with Data Transformation"
status: done
started: 2026-03-29
completed: 2026-03-30
reviewed: true
review_started: 2026-03-30
review_gates_passed: [build, lint, type-check, format-check, unit-tests, e2e-tests-skipped, design-review-skipped, code-review, code-review-testing, performance-benchmark-skipped, security-review, exploratory-qa-skipped]
burn_in_validated: false
---

# Story 59.03: Dexie v29 Schema Migration with Data Transformation

## Story

As a learner with existing flashcards and review records,
I want my SM-2 data automatically migrated to FSRS fields when the app upgrades,
so that the new FSRS scheduling algorithm can operate on my existing learning data without data loss.

## Acceptance Criteria

**Given** existing `flashcards` records with SM-2 fields (`easeFactor`, `interval`, `reviewCount`, `nextReviewAt`, `reviewedAt`)
**When** Dexie upgrades from v30 to v31
**Then** each flashcard record gains FSRS fields (`stability`, `difficulty`, `reps`, `lapses`, `state`, `elapsed_days`, `scheduled_days`, `due`, `last_review`) computed from SM-2 data
**And** legacy SM-2 fields are removed from each record
**And** the `flashcards` index changes from `nextReviewAt` to `due`

**Given** existing `reviewRecords` with SM-2 fields (`easeFactor`, `interval`, `reviewCount`, `nextReviewAt`, `reviewedAt`)
**When** Dexie upgrades from v30 to v31
**Then** each review record gains FSRS fields (`stability`, `difficulty`, `reps`, `lapses`, `state`, `elapsed_days`, `scheduled_days`, `due`, `last_review`) computed from SM-2 data
**And** legacy SM-2 fields are removed from each record
**And** the `reviewRecords` index changes from `nextReviewAt, reviewedAt` to `due, last_review`

**Given** a fresh install (no existing data)
**When** the app creates the database
**Then** the checkpoint schema (v31) is used directly without running migration callbacks

**Given** the migration runs on existing data
**When** SM-2 fields are converted
**Then** `easeFactor` maps to `difficulty` (inverted scale), `interval` maps to `stability`, `reviewCount` maps to `reps`, `nextReviewAt` maps to `due`, `reviewedAt` maps to `last_review`
**And** cards with `reviewCount === 0` get state=0 (New), cards with `interval < 1` get state=1 (Learning), all others get state=2 (Review)

**Given** the schema-checkpoint test
**When** comparing migration-built vs checkpoint-built schemas
**Then** both produce identical table structures at v31

## Tasks / Subtasks

- [x] Task 1: Add Dexie v31 migration with updated indexes for flashcards and reviewRecords (AC: 1, 2)
  - [x] 1.1 Update flashcards index: replace `nextReviewAt` with `due`
  - [x] 1.2 Update reviewRecords index: replace `nextReviewAt, reviewedAt` with `due, last_review`
- [x] Task 2: Write upgrade callback to transform SM-2 data to FSRS fields (AC: 1, 2, 4)
  - [x] 2.1 Transform flashcard records
  - [x] 2.2 Transform reviewRecord records
- [x] Task 3: Update checkpoint.ts to v31 with new indexes (AC: 3)
- [x] Task 4: Update schema-checkpoint.test.ts for v31 (AC: 5)
- [x] Task 5: Write migration-specific unit tests (AC: 1, 2, 4)
- [x] Task 6: Verify build passes

## Implementation Notes

- Migration version is v31 (not v29 as the sprint-status slug suggests — v29 and v30 were already used by E43-S06/notifications and E89-S01/courses-drop respectively)
- SM-2 easeFactor (1.3-2.5, higher=easier) is mapped to FSRS difficulty (0-10, higher=harder) via linear inversion with clamping
- SM-2 interval (days) maps directly to FSRS stability (days) since both represent memory strength duration
- Card state is derived: reviewCount=0 -> New(0), interval<1 -> Learning(1), else -> Review(2). Relearning(3) cannot be inferred from SM-2 data.
- `lapses` is set to 0 for all migrated cards since SM-2 doesn't track forgetting events
- The v31 migration only redeclares the two tables whose indexes change (flashcards, reviewRecords) — Dexie preserves all undeclared tables automatically
- Stores still reference SM-2 field names (expected type errors) — those are fixed in E59-S04

## Testing Notes

- 10 migration-specific tests in `migration-v31-fsrs.test.ts` covering:
  - Full field transformation for reviewed flashcards and review records
  - New/never-reviewed cards (state=0, no last_review)
  - Learning-phase cards (interval < 1, state=1)
  - Index queries using `due` and `last_review`
  - Missing/undefined SM-2 fields (defaults applied)
  - Extreme easeFactor clamping
  - Empty tables (no-op migration)
  - Other table preservation during migration
- Existing `schema.test.ts` flashcard tests updated from SM-2 to FSRS field names
- `schema-checkpoint.test.ts` validates v31 checkpoint matches migration-built schema
- All 70 tests pass across 3 test files

## Pre-Review Checklist

- [ ] All changes committed (`git status` clean)
- [ ] No error swallowing — catch blocks log AND surface errors
- [ ] Read [engineering-patterns.md](../engineering-patterns.md) for full patterns reference

## Design Review Feedback

[N/A — no UI changes in this story]

## Code Review Feedback

[Populated by /review-story]

## Challenges and Lessons Learned

- Migration version is v31 (not v29) because v29/v30 were already consumed by prior stories (E43-S06 notifications, E89-S01 courses-drop). Story slug predates version allocation.
- SM-2 to FSRS field mapping required careful scale inversion for difficulty (SM-2 higher = easier, FSRS higher = harder). Linear inversion with clamping to valid ranges prevents edge-case data corruption.
- Round 1 code review caught non-deterministic `Date.now()` usage in migration callbacks — fixed by capturing a single `migrationNow` timestamp at callback entry and sharing across all records.
- Dexie's `.version().stores().upgrade()` chain only needs to redeclare tables whose indexes change — undeclared tables are preserved automatically, keeping the migration minimal.
