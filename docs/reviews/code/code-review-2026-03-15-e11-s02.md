## Code Review: E11-S02 — Knowledge Retention Dashboard

### What Works Well

1. **Pure function architecture with `now: Date` injection.** Every metric function in `retentionMetrics.ts` accepts `now` as a parameter, making unit tests trivial and E2E tests deterministic via `page.clock.install()`.

2. **Proper async cleanup in the page component.** The `useEffect` in `RetentionDashboard.tsx` (line 23-30) uses the `ignore` flag pattern correctly, and the `Promise.all` has a `.catch()` that respects the cleanup flag.

3. **Comprehensive unit test coverage.** `retentionMetrics.test.ts` covers all public functions including boundary values (50%, 80% thresholds), empty inputs, soft-deleted notes, and all three decay alert types.

### Findings

#### Blockers

None.

#### High Priority

- **`TopicRetentionCard.tsx:76` (confidence: 82)**: Inline `style={{ width: ... }}` on the progress bar violates the project's no-inline-styles ESLint rule. Fix: Add ESLint disable comment documenting that dynamic percentage widths require it.

- **`RetentionDashboard.tsx:20` (confidence: 75)**: `const now = useMemo(() => new Date(), [])` captures time once on mount and never updates. Fix: Document trade-off with a comment (acceptable for this use case).

- **`story-e11-s02.spec.ts:104` (confidence: 80)**: `.catch(() => {})` silently swallows cleanup errors in `afterEach`. Fix: Replace with `.catch(e => console.warn('[afterEach] studySessions cleanup:', e))`.

#### Medium

- **`retentionMetrics.ts:212-213` (confidence: 72)**: `fourWeekAvg` relies on `NaN || 0` coercion — fragile and non-obvious. Fix: Use explicit guard.

- **`retentionMetrics.ts:154-156, 169-171` (confidence: 65)**: Redundant `s.endTime` filter in helper functions (already pre-filtered by caller).

- **`retentionMetrics.ts:98-100` (confidence: 68)**: `lastReviewedAt` reduction creates `new Date()` in hot loop. Fix: Compare ISO strings directly.

#### Nits

- **`EngagementDecayAlert.tsx:55` (confidence: 60)**: Literal emoji `💡` — consider Lucide `Lightbulb` icon.
- **`TopicRetentionCard.tsx:68` (confidence: 55)**: Check `h-2` progress bar height consistency with other progress bars.
- **`retentionMetrics.ts:245-256` (confidence: 50)**: `formatTimeSinceReview` duplicates time-ago formatting logic.

Issues found: 8 | Blockers: 0 | High: 3 | Medium: 3 | Nits: 3
