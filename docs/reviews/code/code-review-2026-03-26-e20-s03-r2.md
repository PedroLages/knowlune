## Code Review: E20-S03 -- 365 Day Activity Heatmap (Round 2)

**Round 2 revalidation** — verifying all 13 issues from Round 1 (4 HIGH, 5 MEDIUM, 4 NITS) are resolved. No new issues introduced.

### Round 1 Fix Verification

| # | Severity | Issue | Status | Evidence |
|---|----------|-------|--------|----------|
| H1 | HIGH | `today` memoized with `[]` deps — stale past midnight | FIXED | Line 59: `const today = toLocalDateString()` — recomputed every render |
| H2 | HIGH | Inline style object re-created every render | FIXED | Lines 133-140: `useMemo(() => ({...}), [totalWeeks])` |
| H3 | HIGH | `Date` + `toLocaleDateString` in inner render loop (365x) | FIXED | Lines 142-156: Pre-computed `formattedDates` Map via `useMemo` with shared `cellDateFormatter` |
| H4 | HIGH | Month sort order depends on Map insertion order | FIXED | `activityHeatmap.ts:159,167`: Uses `YYYY-MM` key + explicit `sort()` |
| M1 | MEDIUM | `study-log-updated` handler has no debounce | FIXED | Lines 94-116: 300ms `setTimeout` debounce with proper cleanup |
| M2 | MEDIUM | `db.studySessions.toArray()` loads full table | FIXED | Line 68: `.where('startTime').above(cutoff).toArray()` |
| M3 | MEDIUM | E2E tooltip test silently passes with `if` guard | FIXED | `story-e20-s03.spec.ts:105`: `await expect(cellWithActivity).toHaveCount(1)` |
| M4 | MEDIUM | Tooltip assertion accepts "No activity" for seeded data | FIXED | `story-e20-s03.spec.ts:111`: Strict `toContainText('30 min')` |
| M5 | MEDIUM | Locale-formatted string as Map key in monthly summary | FIXED | `activityHeatmap.ts:159`: `date.slice(0, 7)` (YYYY-MM) |
| N1 | NIT | Legend swatches lack `cursor-help` | FIXED | Line 366: `cursor-help` class present |
| N2 | NIT | 365 tab stops — no roving tabindex | FIXED | Lines 158-195: Roving tabindex with arrow key navigation |
| N3 | NIT | Redundant `Array<null>` generic | FIXED | `activityHeatmap.ts:105`: `new Array(firstDayOfWeek).fill(null)` |
| N4 | NIT | Fragile `dayMap` Map reference memo | ADDRESSED | Both `setDayMap` calls create new Map instances — pattern is safe |

### New Findings (Round 2)

#### Blockers

None.

#### High Priority

None.

#### Medium

None.

#### Nits

- **NIT** `src/app/components/reports/ActivityHeatmap.tsx:162` (confidence: 55): `target.role` property access on `HTMLElement` — this is a relatively new DOM property (Chrome 120+, Safari 16.4+). Works fine for the project's ES2020 target and modern browser audience. An alternative would be `target.getAttribute('role')` for broader compatibility, but this is not necessary given the project's browser targets.

- **NIT** `src/app/components/reports/ActivityHeatmap.tsx` — Prettier format diff: The `handleGridKeyDown` callback has a formatting difference between committed code and Prettier output (indentation of the callback body). This is cosmetic only and does not affect functionality. Should be committed as a format fix.

### What Works Well (Round 2)

1. **All Round 1 performance fixes verified**: The `formattedDates` memoization, grid style memoization, and Dexie query filter are well-implemented and address the original concerns precisely.

2. **Roving tabindex implementation is solid**: Arrow key navigation with proper `tabIndex` management, focus moves cleanly between cells. The `handleGridKeyDown` callback correctly handles all four arrow directions with grid-aware step sizes (7 for left/right = previous/next week, 1 for up/down = previous/next day).

3. **Debounce cleanup is correct**: The event handler debounce properly clears the timer on unmount and on dependency changes, preventing memory leaks and stale state updates.

4. **Monthly summary sorting is now deterministic**: Using YYYY-MM keys with explicit sort guarantees chronological order regardless of Map insertion order.

---

**Round 2 Verdict: PASS**

Issues found: 2 NITs (cosmetic only) | Blockers: 0 | High: 0 | Medium: 0
All 13 Round 1 issues verified as fixed. No regressions introduced.
