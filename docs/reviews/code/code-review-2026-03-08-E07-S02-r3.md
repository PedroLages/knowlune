# Code Review: E07-S02 — Recommended Next Dashboard Section (Round 3)

**Review Date**: 2026-03-08
**Reviewed By**: Claude Code (code-review agent)

## What Works Well

1. **Round 2 blocker fully resolved.** Custom event pattern (`PROGRESS_UPDATED_EVENT`) correctly addresses same-tab StorageEvent limitation.
2. **Responsive grid fixed.** `sm:grid-cols-2 lg:grid-cols-3` eliminates cramped layout at 640-767px.
3. **Design token compliance improved.** Empty state uses `bg-brand-soft` and `text-brand`.

## Findings

### Blockers

None.

### High Priority

**H1: AC5 E2E test verifies reload, not same-tab custom event path (confidence: 82)**
- `tests/e2e/story-e07-s02.spec.ts:154-203` — tests localStorage write + reload, not the custom event dispatch.
- The Round 2 fix (custom event) remains untested by E2E.
- Fix: Add test variant that dispatches `window.dispatchEvent(new Event('course-progress-updated'))` without reload.

**H2: `loadSessionStats()` fire-and-forget without `.catch()` (confidence: 78)**
- `src/app/components/RecommendedNext.tsx:63` — Dexie query failure leaves `isLoading` true forever.
- Fix: Add `.catch(console.error)`.

**H3: `getTotalLessons()` vs `course.totalLessons` field divergence risk (confidence: 75)**
- `src/lib/recommendations.ts:11,68` — computes from modules instead of using pre-computed field.
- Fix: Use `course.totalLessons` directly or centralize derivation.

### Medium

**M1: `useMemo` dependency on `sessions` array reference (confidence: 72)**
- Unnecessary re-executions when Zustand replaces array with identical data.

**M2: Double `getAllProgress()` calls per render (confidence: 70)**
- Called in Overview.tsx:48 and inside RecommendedNext via useMemo.

**M3: AC5 test uses inline progress object missing `notes` field (confidence: 72)**
- `tests/e2e/story-e07-s02.spec.ts:184-193` — inconsistent with factory pattern.

### Nits

**N1**: Loading state heading mismatch with `RecommendedNextSkeleton` (confidence: 60).
**N2**: Empty state text "Start a course" vs AC4's "import courses" wording (confidence: 55).

## Summary

Issues: 8 | Blockers: 0 | High: 3 | Medium: 3 | Nits: 2
