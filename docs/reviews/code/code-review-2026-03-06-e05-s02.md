# Code Review: E05-S02 — Streak Pause & Freeze Days (Re-review #3)

**Date**: 2026-03-06

## What Works Well

1. All previous high-priority fixes verified and correct: multi-day pause, freeze-aware longestStreak, consolidated Date(), getCurrentStreak with freeze days, Set-based lookups, theme tokens, role="img", flex-wrap, focus restore
2. Robust freeze day data validation at both read and write layers
3. Excellent unit test coverage for freeze day logic with distinguishing AC7 test

## Findings

### Blockers

1. **Uncommitted implementation (confidence: 98)** — Implementation files exist only in working tree. Expected per workflow — commit happens during /finish-story.

### High Priority

1. **getLongestStreak() does not pass freeze days (confidence: 92)** — `studyLog.ts:161-162`: Standalone function calls `longestStreakFromDays(studyDaysFromLog(getLog()))` without `getFreezeDays()`. Fix: add `getFreezeDays()` as second argument.

2. **calculateStreakFromDate infinite loop on pathological input (confidence: 75)** — `studyLog.ts:182-200`: `while(true)` has no safety bound. Currently safe via max 3 freeze days enforcement, but function signature accepts any `number[]`. Fix: add `let maxWalk = 400` safety bound.

3. **clearStreakPause() called as side effect during calculation (confidence: 82)** — `studyLog.ts:234`: Mutates localStorage and dispatches event from within `currentStreakFromDays()`. Can trigger re-entrant snapshot refreshes.

### Medium

1. **Today-is-freeze-day streak starts from yesterday (confidence: 72)** — `studyLog.ts:238-253`: Could start from today and let backward walk handle freeze days uniformly.

2. **Heatmap div with tabIndex={0} and role="img" (confidence: 78)** — `StudyStreakCalendar.tsx:295`: Focusable non-interactive element creates keyboard navigation bloat (100+ cells). Consider removing tabIndex or using grid navigation.

3. **longestStreakFromDays freeze-aware path O(n*k) complexity (confidence: 68)** — `studyLog.ts:276-289`: Acceptable for localStorage datasets, noted for documentation.

### Nits

1. Dialog focus restore race with Radix — consider `requestAnimationFrame` wrapper (confidence: 65)
2. Inline `style={{ gridTemplateColumns }}` — only inline style, acceptable (confidence: 60)
3. `freezeDayIndices.includes()` in backward walk — could use Set for consistency (confidence: 55)

## Previous Findings Status

- Multi-day pause loses streak: **Fixed** — walks from most recent study day
- longestStreak not freeze-aware: **Fixed** — freeze-aware backward walk path
- Multiple new Date() calls: **Fixed** — single `now` variable
- getCurrentStreak() ignores freeze days: **Fixed** — passes getFreezeDays()
- O(n) includes: **Fixed** — Set-based lookups
- Hardcoded colors: **Fixed** — theme tokens
- Heatmap button semantics: **Fixed** — div with role="img"
- Mobile overflow: **Fixed** — flex-wrap + gap-y-2
- Dialog focus: **Fixed** — freezeTriggerRef with focus restore

---
Findings: 10 | Blockers: 1 (expected) | High: 3 | Medium: 3 | Nits: 3
