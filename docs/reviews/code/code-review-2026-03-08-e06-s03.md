## Code Review: E06-S03 — Challenge Milestone Celebrations

### What Works Well

1. **Clean milestone detection logic** in `src/lib/challengeMilestones.ts`. The `detectChallengeMilestones` function is pure, handles `targetValue <= 0` guard, and correctly filters against `celebratedMilestones`. Easy to unit test.

2. **Good structural reuse of the streak milestone pattern.** The tier config approach (`CHALLENGE_TIER_CONFIG`), toast component, and store integration all mirror the proven `StreakMilestoneToast` pattern from E05-S06, reducing architectural risk.

3. **Proper grouping logic for completed challenges.** The `useMemo` reducer correctly separates `active`, `completed`, and `expired` categories with `isCompleted` taking priority over `deadlinePassed`, which is the right precedence.

### Findings

#### Blockers

- **[`src/app/pages/Challenges.tsx`:132-146] (confidence: 92)**: `fireMilestoneToasts` schedules `setTimeout` timers that are never cleaned up. If the user navigates away from `/challenges` before all staggered toasts fire, the timeouts will execute after unmount. Fix: Return timer IDs and clear them in useEffect cleanup.

#### High Priority

- **[`src/app/components/celebrations/ChallengeMilestoneToast.tsx`:29] (confidence: 85)**: `tierConfig.confettiColors` array literal in useEffect deps causes re-fires on every re-render. Fix: Use `[milestone]` as sole dependency.

- **[`src/stores/useChallengeStore.ts`:78-79] (confidence: 88)**: `refreshAllProgress` returns `milestoneMap` after failed `bulkPut` — milestones toast but never persist, causing duplicates on next load. Fix: Clear milestoneMap in catch block.

- **[`src/app/pages/Challenges.tsx`:154,161] (confidence: 82)**: `hasFiredRef` permanently blocks milestone toasts for entire session after first fire. Fix: Remove guard (DB-level check prevents duplicates) or use per-challenge key.

- **[`src/app/pages/Challenges.tsx`:158-168] (confidence: 80)**: `.then()` chain on `refreshAllProgress()` has no `.catch()`. Fix: Add `.catch(console.error)`.

#### Medium

- **[`src/app/components/celebrations/ChallengeMilestoneToast.tsx`:32-33] (confidence: 72)**: Toast div has `aria-label` but no `role` attribute. Fix: Add `role="status"`.

- **[`src/lib/challengeMilestones.ts`:30,41,52,63] (confidence: 68)**: Hardcoded hex colors for confetti — acceptable since canvas-confetti requires hex strings.

- **[`tests/e2e/story-e06-s03.spec.ts`:725-731] (confidence: 75)**: afterEach IDB cleanup silently swallows errors.

#### Nits

- **[`src/app/pages/Challenges.tsx`:86] (confidence: 65)**: `hover:bg-amber-600` identical to default — no hover feedback.
- **[`src/app/pages/Challenges.tsx`:255] (confidence: 60)**: Duplicate className between Completed/Expired triggers.
- **[`src/lib/challengeMilestones.ts`:70] (confidence: 70)**: Silent fallback for unknown milestone percentages.

---
Issues found: 10 | Blockers: 1 | High: 4 | Medium: 3 | Nits: 3
