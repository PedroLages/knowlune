# Code Review Report — E06-S03: Challenge Milestone Celebrations

**Review Date**: 2026-03-08 (Round 2)
**Reviewed By**: Claude Code (code-review agent)

---

## What Works Well

1. Clean milestone detection logic — pure function, guards against targetValue <= 0, comprehensive unit tests
2. Previous Round 1 findings all addressed (timer cleanup, milestoneMap.clear, .catch, role="status")
3. Proper grouping precedence — completed prioritized over expired in useMemo reducer

## Findings

### High Priority

- **H1 — `celebratedMilestones` undefined crash for pre-existing records (confidence: 88)**
  - Location: `src/lib/challengeMilestones.ts:90`
  - No Dexie migration backfills `celebratedMilestones: []` on existing records from E06-S01/S02
  - Fix: `const celebrated = challenge.celebratedMilestones ?? []` in both `detectChallengeMilestones` and `refreshAllProgress`

- **H2 — `refreshAllProgress` double-call race condition (confidence: 82)**
  - Location: `src/stores/useChallengeStore.ts:49-69`
  - If called twice in quick succession, second call reads stale `celebratedMilestones` → duplicate toasts
  - Fix: Add `isRefreshing` guard or read from DB within transaction

- **H3 — Confetti double-fire on Sonner re-renders (confidence: 78)**
  - Location: `src/app/components/celebrations/ChallengeMilestoneToast.tsx:19-29`
  - Sonner may remount custom toast during animation transitions → confetti fires again
  - Fix: Add `hasFiredRef` guard in useEffect

### Medium

- **M1 — Hardcoded amber Tailwind colors instead of theme tokens (confidence: 75)**
  - Location: `src/app/pages/Challenges.tsx:63,72,86,101`
  - Fix: Add `--color-completed` semantic token or document intentional deviation

- **M2 — `CHALLENGE_TIER_CONFIG` accepts any number key (confidence: 72)**
  - Location: `src/lib/challengeMilestones.ts:22`
  - Fix: Use `Record<25 | 50 | 75 | 100, ChallengeTierConfig>`

- **M3 — `fireMilestoneToasts` creates React elements in timer context (confidence: 70)**
  - Location: `src/app/pages/Challenges.tsx:135-149`
  - Fix: Move confetti to `fireMilestoneToasts`, make toast a pure render component

### Nits

- `sectionTriggerClass` uses string concatenation instead of `cn()` (confidence: 65)
- Hardcoded hex in `confettiColors` — unavoidable but add mapping comment (confidence: 55)
- `completedOpen` defaults to false — consider true for celebration moment (confidence: 60)

---

Findings: 9 | Blockers: 0 | High: 3 | Medium: 3 | Nits: 3
