# Code Review: E63-S02 — Token-Aware Profile Formatter + Orchestrator

**Date:** 2026-04-13
**Reviewer:** Claude Opus (automated)
**Files changed:** `src/ai/tutor/learnerProfileBuilder.ts`

## Summary

S02 adds ~250 lines to the existing learnerProfileBuilder module: signal formatters, topic filtering, token-aware formatter, and a `buildAndFormatLearnerProfile` orchestrator. Clean, well-structured code following the established pure-function pattern from S01.

## Findings

### MEDIUM

1. **Comma-expression side-effect in ternary (lines 553-575)**
   The `console.warn(...), null` pattern uses the comma operator to log and return null in a single expression. While functional, this is an unusual pattern that can confuse readers and some linters may flag it. A standard if/else or helper function would be clearer.
   - File: `src/ai/tutor/learnerProfileBuilder.ts:553-575`

### LOW

2. **`SIGNAL_PRIORITY` exported but unused (line 96-101)**
   The `SIGNAL_PRIORITY` constant is exported but never referenced in code. The priority order is implicitly encoded in `formatLearnerProfile`'s if-block ordering. Either reference it in the formatter or remove it to avoid drift.
   - File: `src/ai/tutor/learnerProfileBuilder.ts:96-101`

3. **`signal` field in signalBlocks unused (line 475)**
   Each signal block stores `{ signal: ProfileSignal, text: string }` but the `signal` field is never read. It adds type safety for future use but is dead code today.
   - File: `src/ai/tutor/learnerProfileBuilder.ts:475`

### NIT

4. **Forced inclusion bypasses budget contract (lines 514-516)**
   When the budget is extremely tight, the first signal is always included regardless of budget. This contradicts the documented contract "approximately N tokens or fewer." Consider returning empty string instead, or documenting this as intentional.
   - File: `src/ai/tutor/learnerProfileBuilder.ts:514-516`

## Verdict

**PASS** — No blockers or high-severity issues. 1 medium (stylistic), 2 low, 1 nit.
