# Code Review: E29-S03 — Fix CareerPaths Mislabelling and Add Sidebar Nav Entry

**Date:** 2026-03-27
**Reviewer:** Claude (automated)
**Branch:** `feature/e29-s03-fix-careerpaths-mislabelling-and-add-sidebar-nav-entry`
**Verdict:** ISSUES FOUND

## Files Changed

| File | Change |
|------|--------|
| `src/app/config/navigation.ts` | Added "Career Paths" sidebar nav entry with Compass icon |
| `src/app/pages/CareerPaths.tsx` | Changed 2 headings from "Learning Paths" to "Career Paths" |
| `src/app/pages/CareerPathDetail.tsx` | Changed back-link aria-label and badge text |
| `docs/implementation-artifacts/29-3-*.md` | Story file |

## BLOCKER Issues

### B1: Incomplete mislabel fix — 6 remaining "learning path" references in CareerPaths.tsx

The story's primary goal is to fix mislabelling, but 6 instances of "learning path(s)" remain in `CareerPaths.tsx`:

| Line | Text | Context |
|------|------|---------|
| 221 | `'Failed to load learning paths. Please try again.'` | Error message |
| 315 | `aria-label="Search learning paths"` | Search input aria-label |
| 331 | `No learning paths found for "${search}"...` | Empty search state |
| 337 | `"No learning paths available"` | Empty state title |
| 338 | `"Curated learning paths will appear here..."` | Empty state description |
| 346 | `aria-label="Learning paths"` | List aria-label |

**Impact:** Screen readers will announce "learning paths" in these contexts, directly contradicting AC1 ("screen readers announce 'Career Paths' as the page title") and the general mislabelling fix intent. Lines 315, 346 are especially critical as they are accessibility-facing.

**Fix:** Replace all 6 instances with "career path(s)" equivalents.

### B2: Unit tests not updated — 2 failing tests in navigation.test.ts

The story added a new "Career Paths" entry to the Library navigation group but did not update `src/app/config/__tests__/navigation.test.ts`:

- **Line 10:** Test expects 4 Library items, now there are 5
- **Line 12-17:** Expected items array missing "Career Paths"
- **Line 70:** Overflow count expects 13, now 14

**Impact:** Unit test suite fails (2 failures out of 3180 tests).

### B3: E2E tests not updated — 2 failing tests in error-states.spec.ts

`tests/e2e/error-states.spec.ts` still expects `heading { name: 'Learning Paths' }`:

- **Line 42:** `getByRole('heading', { name: 'Learning Paths' })` — should be "Career Paths"
- **Line 83, 92:** Same heading expectation in search empty state test

**Impact:** E2E suite has 2 broken tests for career paths functionality.

## HIGH Issues

None.

## MEDIUM Issues

### M1: Regression test spec also has stale "Learning Paths" references

`tests/e2e/regression/career-paths.spec.ts` has 5 references to "Learning Paths"/"Learning paths" (lines 25, 42, 241, 247, 249) that need updating. These tests are gated behind `RUN_REGRESSION` env var so they don't fail in default runs, but they will fail when regression tests are run.

## LOW Issues

None.

## Architecture & Security

No concerns — this is a text/config change with no architectural implications.
