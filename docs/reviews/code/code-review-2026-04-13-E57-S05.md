# Code Review — E57-S05 RAG-Grounded Answers

**Date:** 2026-04-13
**Reviewer:** Claude Opus (automated)
**Verdict:** ISSUES FOUND

## BLOCKER

### B1: schema.test.ts not updated for v50

`src/db/__tests__/schema.test.ts` still asserts `version 49` (line 101) and does not include `transcriptEmbeddings` in the expected table list. The story bumped CHECKPOINT_VERSION to 50 and added the table but forgot to update this test.

**Files:** `src/db/__tests__/schema.test.ts:101` (version assertion), table list assertion

### B2: Unused import causes TypeScript error

`src/ai/tutor/__tests__/transcriptChunker.test.ts:2` imports `TranscriptChunk` but never uses it, causing `TS6133`. This will fail strict CI type checks.

**File:** `src/ai/tutor/__tests__/transcriptChunker.test.ts:2`

## MEDIUM

### M1: Dead-code infinite loop guard in transcriptChunker.ts

Line 87: `if (cueIndex <= cueIndex - advance) break` — since `advance = Math.max(1, ...)` is always >= 1, `cueIndex` can never be <= `cueIndex - advance`. The guard is logically impossible to trigger. Should be removed or replaced with a meaningful check (e.g., tracking previous `cueIndex` value).

**File:** `src/ai/tutor/transcriptChunker.ts:87`

### M2: Timeout promise leak in tutorRAG.ts

The `setTimeout` in `retrieveTutorContext` (line ~82) creates a timer that is never cleared when `generateEmbeddings` resolves first. While the leaked timer is short-lived (10s), it's a resource leak. Use `AbortController` or clear the timer on success.

**File:** `src/ai/tutor/tutorRAG.ts:82-84`

## LOW

### L1: `new Date().toISOString()` in transcriptEmbedder.ts

Line 98 uses `new Date()` for `createdAt`. This is production code (not test code) so the ESLint `deterministic-time` rule doesn't apply, but it means embeddings will have slightly different timestamps per batch rather than a single consistent timestamp for the entire embedding operation. Minor consistency issue.

**File:** `src/ai/tutor/transcriptEmbedder.ts:98`

## Summary

| Severity | Count |
|----------|-------|
| BLOCKER  | 2     |
| HIGH     | 0     |
| MEDIUM   | 2     |
| LOW      | 1     |
| NIT      | 0     |
| TOTAL    | 5     |
