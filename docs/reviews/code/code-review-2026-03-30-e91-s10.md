# Code Review: E91-S10 Course Hero Overview Page

**Date:** 2026-03-30
**Story:** E91-S10 — Course Hero Overview Page
**Reviewer:** Claude Opus 4.6 (automated)

## Files Reviewed

| File | Lines Changed | Verdict |
|------|--------------|---------|
| `src/app/pages/CourseOverview.tsx` | +733 (new) | PASS |
| `src/app/components/course/CourseHeader.tsx` | +26 | PASS |
| `src/app/routes.tsx` | +5 | PASS |
| `src/lib/formatDuration.ts` | +37 (new) | PASS |
| `tests/e2e/story-e91-s10.spec.ts` | +175 (new) | PASS |

## Findings

### No Blockers

### HIGH

None.

### MEDIUM

1. **Bundle regression (informational)** — Total JS bundle grew 16.4% vs baseline. The `index` chunk grew 440KB. This is driven by existing dependencies (sql.js, chart.js) not by this story's code. The new `CourseOverview` page is properly lazy-loaded. No action required for this story.

### LOW

1. **Inline styles for gradients** — Three `style={}` attributes used for CSS custom property gradients. Each has an ESLint disable comment explaining the necessity (dynamic gradients cannot use Tailwind classes). Acceptable pattern.

2. **`silent-catch-ok` comment** — Used for `youtubeChapters` table query that may not exist for local courses. Pattern is consistent with project conventions.

### No Issues Found

- **Architecture**: Clean adapter pattern usage, never checks `course.source` directly
- **Accessibility**: Loading skeleton has `role="status"` and `aria-busy`, icons have `aria-hidden`, semantic HTML throughout
- **Security**: No user input handling, no API calls, no secrets
- **Error handling**: Proper error boundaries, toast notifications for CTA failures, console.error for debugging
- **Memory management**: Blob URL cleanup via `revokeObjectUrl` in effect cleanup, ignore flags prevent stale state updates
- **Shared utility**: `formatDuration` extracted to `src/lib/formatDuration.ts` for reuse across components
