## Code Review: E112-S01 — Reading Speed, ETA & Time-of-Day Patterns (Round 2)

### What Works Well

1. **Clean R1 fix commit.** All R1 findings were addressed in a single focused commit: ETA formula corrected (divide by 30 days), speed inflation fixed by tracking books with recent sessions, AC2 UI wired up, and comprehensive behavioral tests added.

2. **Solid error handling throughout.** Both `ReadingStatsSection` and `ReadingPatternsCard` use `silent-catch-ok` annotated catch blocks with `console.error` and graceful degradation (zero-state fallback or null render). The `usePagesReadToday` hook correctly uses an ignore ref for unmount safety.

3. **Thorough test coverage.** The test suite covers boundary conditions well: time bucket edges (hour 5, 11, 21, 0, 4), midnight wrap for Night, sub-30-second session filtering, progress cap behavior, and null/empty data paths. The `FIXED_DATE` pattern is used correctly throughout.

### Findings

#### Blockers

None.

#### High Priority

None.

#### Medium

- **[Correctness] `src/services/ReadingStatsService.ts:154` (confidence: 75)**: Unreachable singular "week" code path. The `weeks === 1 ? 'week' : 'weeks'` ternary on line 154 can never produce "1 week" because the weeks format only triggers when `etaDays > 14` (line 152), and `Math.ceil(15/7) = 3` is the minimum possible value. Why: Dead code increases cognitive load and suggests untested behavior. Fix: Either remove the singular branch (always use "weeks") or lower the threshold to `> 7` so that 8-14 days maps to "1-2 weeks" instead of showing raw days. ~5 min.

#### Nits

- **Nit [Testing] `src/services/__tests__/ReadingStatsService.test.ts:506` (confidence: 70)**: Misleading test name. The test is titled "returns '1 week' (singular) for exactly 7-day ETA" but the extensive inline commentary (lines 507-541) concludes that "1 week" is unreachable, and the test actually asserts `'3 weeks'`. The test itself is correct but the `it()` description is confusing. Fix: Rename to something like `'returns weeks format for ETAs > 14 days (singular week is unreachable)'`. ~1 min.

### Recommendations

1. Fix the misleading test name (Nit) -- trivial, 1 minute.
2. Decide on the unreachable "1 week" path (Medium) -- either accept as-is (harmless dead code) or adjust the threshold. Not blocking.

---
Issues found: 2 | Blockers: 0 | High: 0 | Medium: 1 | Nits: 1
Confidence: avg 73 | >= 90: 0 | 70-89: 2 | < 70: 0
