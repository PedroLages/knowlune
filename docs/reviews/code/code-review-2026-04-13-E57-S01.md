# Code Review: E57-S01 Tutor Chat UI + Context Injection

**Date:** 2026-04-13
**Reviewer:** Claude Opus 4.6 (automated)
**Branch:** main (current HEAD)

## Summary

E57-S01 adds a tutor chat UI with transcript context injection, including three transcript extraction strategies (full, chapter, window), a 6-slot priority prompt builder, TranscriptBadge, TutorChat orchestrator, TutorEmptyState, a standalone Tutor page, and sidebar navigation entry.

## Findings

### BLOCKER

None.

### HIGH

**H1: Missing unit tests for transcriptContext.ts and tutorPromptBuilder.ts**
Story tasks 2.6 and 3.4 explicitly require unit tests for all 3 transcript strategies, edge cases, slot priority, and budget trimming. No test files exist.
- Files: `src/ai/tutor/transcriptContext.ts`, `src/ai/tutor/tutorPromptBuilder.ts`

**H2: No error handling in handleSend async callback**
`TutorChat.handleSend` calls `getTranscriptContext()` (async DB access) without try/catch. If the DB call throws, the error propagates unhandled to the user with no feedback.
- File: `src/app/components/tutor/TutorChat.tsx:81-85`

### MEDIUM

**M1: Unused `useState` import in Tutor.tsx**
TypeScript error: `useState` imported but never used. Causes `tsc --noEmit` failure for this file.
- File: `src/app/pages/Tutor.tsx:9`

**M2: Duplicate lucide-react import statement in Tutor.tsx**
Two separate import lines from `lucide-react` (lines 10 and 14). Should be consolidated.
- File: `src/app/pages/Tutor.tsx:10,14`

**M3: Duplicated JSX in TutorChat (empty vs messages state)**
The component renders nearly identical wrapper divs for empty and non-empty states (lines 127-141 vs 144-157). The TranscriptBadge header and ChatInput are duplicated.
- File: `src/app/components/tutor/TutorChat.tsx:126-158`

### LOW

**L1: `windowCues[windowCues.length - 1].endTime` may be undefined**
`TranscriptCue.endTime` could be undefined if the last cue has no end time. `formatTime(undefined)` would produce `NaN:NaN`.
- File: `src/ai/tutor/transcriptContext.ts:236`

**L2: Extra blank line in routes.tsx**
Two consecutive blank lines after the Tutor lazy import.
- File: `src/app/routes.tsx` (around line 120-121 in the diff)

## Pre-existing Issues (not introduced by this story)

- TypeScript errors in ReadingPatternsCard.test.tsx, GenreDistributionCard.tsx, YouTubePlayer.tsx, settings.test.ts, etc.
- BelowVideoTabs.tsx exceeds 300-line component size warning (334 lines) — partially exacerbated by this story's additions
- routes.tsx exceeds 500-line component size warning (549 lines) — pre-existing

## Verdict

Issues found. H1 (missing tests) and H2 (unhandled error) should be addressed before merging.
