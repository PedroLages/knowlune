## Code Review: E60-S01 -- Knowledge Decay Alert Trigger

### What Works Well

1. **Exact pattern replication.** The implementation mirrors `checkSrsDueOnStartup()`, `hasSrsDueToday()`, and the `srs:due` handler almost mechanically -- same fire-and-forget pattern, same `toLocaleDateString('sv-SE')` dedup, same `silent-catch-ok` annotation. This consistency makes the notification system predictable and easy to extend for future event types.

2. **All critical guardrails respected.** Checkpoint stays at v31 (confirmed `src/db/checkpoint.ts` unchanged), v32 migration uses `modify()` with `undefined` guard, dedup filters on `metadata.topic` not just type, `getTopicRetention` imported from the correct module, and the dedup check runs before `store.create()`. Every guardrail in the story file was followed.

3. **Clean working tree.** All changes are committed. The recurring uncommitted-fix pattern (10 consecutive stories) is not present here -- a positive trend that should be acknowledged.

4. **Schema test updated correctly.** The checkpoint test assertion was relaxed from `toBe` to `toBeGreaterThanOrEqual` with a clear comment explaining that data-only migrations above the checkpoint are valid. The version test updated from 31 to 32. Both changes are correct and well-reasoned.

### Findings

#### Blockers

None.

#### High Priority

None.

#### Medium

None.

#### Nits

- **Nit** `src/services/NotificationService.ts:25` (confidence: 75): `DECAY_THRESHOLD = 50` duplicates the value of `FADING_THRESHOLD = 50` in `src/lib/retentionMetrics.ts`. Both represent the same boundary (retention below 50% triggers a "fading" classification). Currently the value is hardcoded in two places. If the threshold ever changes, both must be updated in lockstep. Consider importing from a shared constant in a future refactor. [Category: Maintainability]

- **Nit** `src/app/pages/Notifications.tsx:231` (confidence: 95, pre-existing): String interpolation `className={... ${iconColor}}` instead of `cn()` utility. This is the recurring `cn()` pattern (17+ instances since E01-S03). Not introduced by this story but touched by it (the new `knowledge-decay` type adds `text-warning` to `notificationIconColors`, which flows through this interpolation). [Category: Maintainability] [Recurring]

### Recommendations

1. No action required for this story. The implementation is clean, follows all existing patterns, and correctly implements all six acceptance criteria.

2. Future consideration: Extract the retention threshold to a shared constant (e.g., `RETENTION_DECAY_THRESHOLD` in a constants file) that both `retentionMetrics.ts` and `NotificationService.ts` import. This is a maintainability improvement, not a correctness issue.

---

Issues found: 2 | Blockers: 0 | High: 0 | Medium: 0 | Nits: 2
Confidence: avg 85 | >= 90: 1 | 70-89: 1 | < 70: 0
