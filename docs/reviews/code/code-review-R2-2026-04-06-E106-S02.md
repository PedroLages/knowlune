# Code Review — E106-S02 (Round 2)

**Date:** 2026-04-06
**Branch:** feature/e106-s02-lib-service-coverage
**Reviewer:** Claude Opus 4.6 (code-review agent)
**Round:** 2 (10 fixes applied in R1)

## Quality Gate Results

| Gate | Result |
|------|--------|
| Build | PASS |
| Lint | PASS (0 errors, 102 warnings — all pre-existing, KI-033) |
| TypeScript | 64 errors — all pre-existing (none in story-changed files) |
| Unit Tests | 248/258 files pass, 4358/4399 tests pass — 10 failing files all pre-existing |
| Story Tests | All 6 story-changed test files pass (isolated run: 55/55 ABS, others pass) |

## Files Changed (Story Commits Only)

R1 fix commit `60ae196d`:
- `src/lib/__tests__/avatarUpload.test.ts` — removed unused `vi` import
- `src/lib/__tests__/dashboardOrder.test.ts` — fixed TS cast with `as unknown as`
- `src/lib/__tests__/dataPruning.test.ts` — removed unused imports
- `src/services/__tests__/AudiobookshelfService.test.ts` — updated proxy assertions, eslint suppress
- `src/services/__tests__/OpfsStorageService.test.ts` — removed unused `db` import
- `src/services/__tests__/ReadingStatsService.test.ts` — removed unused `toLocalDateString` import

## Findings

### STORY-RELATED

None. All R1 findings have been addressed correctly.

### PRE-EXISTING (from merged bugfix commit 4cb14e70)

1. **[LOW] Unused `next` parameter** — `server/index.ts:227`: The `app.use('/api/abs/proxy', ...)` middleware declares `next` but never calls it. Should be removed or prefixed with `_`.

2. **[LOW] Stale comment** — `server/index.ts:160`: Comment says "10 req/min" but actual rate limit is `max: 60` (60 req/min).

3. **[LOW] Dead code: `/api/abs/ping` route** — `server/index.ts:176`: The dedicated ping endpoint is never called by the client (which uses `/api/abs/proxy/api/authorize` instead). Could be removed.

### KNOWN ISSUES (matched)

- KI-033: 102 ESLint warnings (pre-existing)
- KI-036: Unit test coverage at 60.31% (improved from 57.07%, still below 70% target)

## Verdict

**PASS** — All R1 findings addressed. No story-related issues remain.
