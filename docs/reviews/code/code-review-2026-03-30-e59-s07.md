# Code Review - E59-S07 "Unit Test Rewrite for FSRS Algorithm"

**Date:** 2026-03-30
**Reviewer:** Claude (automated)
**Files Reviewed:**
- `src/lib/__tests__/spacedRepetition.test.ts` (complete rewrite, 478 lines)
- `docs/implementation-artifacts/stories/E59-S07.md` (new story file)
- `docs/implementation-artifacts/sprint-status.yaml` (status update)

---

## Summary

Complete rewrite of the spaced repetition unit tests from SM-2 to FSRS. The test file now contains 38 tests across 6 describe blocks covering: new card + each rating, review card + each rating, edge cases (boundary difficulty, undefined last_review), predictRetention power-law decay validation, isDue with due field, and full review flow integration tests. All tests use `fsrsTest` (deterministic, no fuzz) and `FIXED_DATE` constant per project ESLint rules.

**Verdict:** 0 BLOCKER, 0 HIGH, 1 MEDIUM, 2 LOW

---

## MEDIUM

### M1. Prettier formatting not applied to story test file

**File:** `src/lib/__tests__/spacedRepetition.test.ts`

The story's test file fails `prettier --check`. This should be fixed before merge to keep CI green.

**Classification:** STORY-RELATED

**Fix:** Run `npx prettier --write src/lib/__tests__/spacedRepetition.test.ts`

---

## LOW

### L1. Story file has placeholder sections

**File:** `docs/implementation-artifacts/stories/E59-S07.md:52-56`

The Implementation Notes, Testing Notes, and Challenges/Lessons Learned sections still contain placeholder text (`[Architecture decisions...]`, `[Test strategy...]`, `[Document issues...]`). These should be filled in before the story is marked complete.

**Classification:** STORY-RELATED

### L2. Coverage threshold failure (pre-existing)

The global unit test coverage threshold (70% lines) fails at 69.32%. This is not caused by this story — the test rewrite maintains equivalent or better coverage of `spacedRepetition.ts`. The threshold failure comes from other files (e.g., `isPremium.ts` at 15.38%, `usePrefsStore.ts` at 28.84%).

**Classification:** PRE-EXISTING

---

## Architecture Assessment

**Positive patterns observed:**
- Single gateway pattern respected — tests import only from `../spacedRepetition`, not directly from `ts-fsrs`
- `fsrsTest` instance with `enable_fuzz: false` ensures deterministic assertions
- `FIXED_DATE` constant avoids non-deterministic time (ESLint rule compliant)
- `makeCardState()` factory helper reduces boilerplate and improves readability
- Integration tests verify multi-step review flows (new -> good -> good -> good)
- Power-law decay verification at `t=S` checks R ~= 90% (FSRS design property)

**No security concerns** — pure unit tests with no I/O, no secrets, no external calls.
