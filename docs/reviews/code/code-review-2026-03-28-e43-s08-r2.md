# Code Review: E43-S08 Auth UX Polish (Round 2)

**Date:** 2026-03-28
**Reviewer:** Claude Opus 4.6 (automated)
**Story:** E43-S08 — Auth UX Polish
**Branch:** feature/e43-s08-auth-ux-polish
**Round:** 2 (verification of Round 1 fixes + fresh analysis)

## Round 1 Fix Verification

All 3 Round 1 issues confirmed fixed in commit `e3cbb10e`:

1. **HIGH: getSession mock missing** — Fixed. Mock added at `useAuthLifecycle.test.ts:20`. All 6 tests pass.
2. **MEDIUM: Invalid ESLint disable directive** — Fixed. `eslint-disable-next-line react-hooks/exhaustive-deps` removed from `Login.tsx:37`.
3. **LOW: Prettier formatting** — Fixed for `settings.ts` and `settings.test.ts`. However, Prettier reformatted 2 additional story files (see LOW below).

## STORY-RELATED Issues

### LOW: Uncommitted Prettier formatting in 2 story files

**Files:** `src/app/hooks/useAuthLifecycle.ts`, `src/app/hooks/__tests__/useAuthLifecycle.test.ts`

Both files have minor Prettier line-wrapping differences (single-line vs multi-line formatting). The committed versions pass `npx prettier --check` but use a slightly different wrapping style than Prettier's preferred output. Running `npx prettier --write` produces cosmetic changes.

**Impact:** No functional impact. These are style-only differences that Prettier auto-fixes.

**Fix:** Run `npx prettier --write src/app/hooks/useAuthLifecycle.ts src/app/hooks/__tests__/useAuthLifecycle.test.ts` and commit.

## PRE-EXISTING Issues (not in story scope)

- TypeScript errors in `src/services/__tests__/NotificationService.test.ts` (3 errors — generic type mismatch)
- Unit test failures in `src/lib/entitlement/__tests__/isPremium.test.ts` (8 failures)
- ESLint `no-silent-catch` warnings in `Layout.tsx` lines 275, 307 (localStorage parse catch blocks — legitimately silent)
- Multiple ESLint unused-variable warnings in test files (auth-flow, account-management, error-states, premium-gating, youtube-import)

## Positive Observations

1. **All Round 1 issues resolved** — Clean fix commit, no regressions introduced
2. **Security posture solid**: HTTPS-only avatar URL guard, dual CSP update, referrerPolicy
3. **Radix Avatar pattern correct**: Sibling AvatarImage + AvatarFallback (not ternary) enables proper fallback on load failure
4. **Race condition handling**: `ignore` flag + subscription-before-getSession ordering follows Supabase best practices exactly
5. **onAuthStateChange callback remains synchronous**: No async/await inside callback (prevents Supabase deadlocks per auth-js#762)
6. **Test coverage**: 55 story-related tests pass (13 new Google OAuth hydration + 6 auth lifecycle + existing settings)
7. **Hydration idempotency**: `hydrateSettingsFromSupabase()` safely called from both `INITIAL_SESSION` event and `getSession()` without double-writing
8. **User preference precedence**: Custom photo (data: URL) > Google avatar (https: URL) > initials fallback — well-documented and tested

## Verdict

**PASS** — One LOW cosmetic formatting issue. All functional code is correct, secure, and well-tested.
