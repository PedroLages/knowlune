# Code Review: E29-S02 — Add Error Handling to useLearningPathStore Mutations

**Date:** 2026-03-27
**Reviewer:** Claude Opus 4.6 (automated)
**Branch:** `feature/e29-s02-add-error-handling-to-uselearningpathstore-mutations`
**Files changed:** `src/stores/useLearningPathStore.ts`, `docs/implementation-artifacts/29-2-*.md`

## Summary

Story adds try/catch + rollback + toast.error to 5 mutations in `useLearningPathStore.ts`:
`createPath`, `renamePath`, `updateDescription`, `addCourseToPath`, `removeCourseFromPath`.

Pattern matches the established convention in `useFlashcardStore`, `useSessionStore`, etc.

## Story-Related Issues

### MEDIUM

1. **`addCourseToPath` captures `prevEntries`/`prevPaths` AFTER the early return guard** (line 214-215) — If `prevEntries` and `prevPaths` are captured after the duplicate-course guard at line 199-201, they're fine since the guard returns early and no state mutation occurs before capture. No issue here upon closer inspection. **RESCINDED.**

### LOW

1. **`createPath` re-throws after toast.error** (line 104) — This is intentional per lessons learned (callers like `generatePath` need the error), but the other 4 mutations silently swallow. This asymmetry is documented but could confuse future maintainers. Consider adding a code comment at the `throw error` line explaining why. **File: `src/stores/useLearningPathStore.ts:104`**

### NITS

1. **Long rollback `set()` lines** — Lines 102, 131, 159, 235, 281 each pack rollback state + error message into a single line. Breaking these into multi-line objects would improve readability. **File: `src/stores/useLearningPathStore.ts:102,131,159,235,281`**

## Pre-Existing Issues (NOT changed by this story)

### HIGH

1. **`deletePath` has no error handling** (line 164-181) — If `persistWithRetry` throws, the path is deleted from DB but state update at line 172 may or may not run depending on where the error occurs. No rollback, no toast. **File: `src/stores/useLearningPathStore.ts:164-181`**

2. **`reorderCourse` uses `.catch()` without rollback** (line 323-326) — Sets error state but does NOT rollback the optimistic update, leaving UI out of sync with DB. No `toast.error`. **File: `src/stores/useLearningPathStore.ts:313-327`**

3. **`applyAIOrder` uses `.catch()` without rollback** (line 509-512) — Same pattern as reorderCourse: sets error but no rollback, no toast. **File: `src/stores/useLearningPathStore.ts:498-512`**

### MEDIUM

1. **`clearPath` sets error but no `toast.error()`** (line 459) — Inconsistent with the pattern established by this story. **File: `src/stores/useLearningPathStore.ts:453-460`**

2. **`generatePath` sets error but no `toast.error()`** (line 432-435) — Uses `set({ error })` but no user-visible toast notification. **File: `src/stores/useLearningPathStore.ts:425-436`**

## Verdict

**PASS** — The 5 mutations in scope are correctly wrapped with the established try/catch + rollback + toast.error pattern. No blockers or high-severity issues in story-changed code.
