# Code Review: E03-S07 — Bookmarks Page (Round 2)

**Date**: 2026-03-01
**Files reviewed**: `src/app/pages/Library.tsx`, `src/lib/bookmarks.ts`, `tests/e2e/story-e03-s07.spec.ts`, `src/lib/__tests__/bookmarks.test.ts`
**Diff**: `git diff HEAD -- src/app/pages/Library.tsx src/lib/bookmarks.ts`

## What Works Well

1. **Round 1 fixes applied**: Keyboard accessibility, delete button touch target, Dexie index-based sorting, size-* shorthand, E2E title assertions — 7 of 12 findings addressed
2. **Clean `getAllBookmarks()`**: Uses `db.bookmarks.orderBy('createdAt').reverse().toArray()` — correct Dexie pattern with indexed column
3. **Unit test added**: `bookmarks.test.ts` covers empty, sort order, cross-course scenarios with proper IndexedDB isolation

## Findings

### Blockers

1. **All implementation code is uncommitted** (confidence: 99)
   - `Library.tsx:BookmarksSection`, `bookmarks.ts:getAllBookmarks()`, unit test, E2E test updates — all exist only in working tree
   - If merged as-is, all 9 E2E tests would fail
   - Fix: `git add src/app/pages/Library.tsx src/lib/bookmarks.ts src/lib/__tests__/bookmarks.test.ts tests/e2e/story-e03-s07.spec.ts && git commit`

### High Priority

2. **`handleDelete` has no catch block** (`Library.tsx:86-92`, confidence: 90)
   - If `deleteBookmark()` rejects, the promise propagates as unhandled
   - `finally` closes the dialog but user gets no error feedback; bookmark reappears on next visit
   - Fix: Add catch block with `toast.error()` and only remove from local state on success

3. **`getAllBookmarks` error silently swallowed** (`Library.tsx:80-84`, confidence: 88)
   - `.catch(() => { setIsLoading(false) })` shows empty state instead of error state
   - User with bookmarks sees "No bookmarks yet" on IndexedDB error — thinks bookmarks are deleted
   - Fix: Add error state and display error message

4. **Nested interactive elements** (`Library.tsx:125-157`, confidence: 85)
   - Delete `<Button>` is nested inside `<div role="button" tabIndex={0}>`
   - Creates confusing keyboard navigation and screen reader experience
   - Fix: Restructure to avoid nested interactive elements

### Medium

5. **Hardcoded yellow color tokens** (`Library.tsx:134-135`, confidence: 72)
   - `bg-yellow-100`, `text-yellow-800` etc. bypass theme.css semantic tokens
   - Fix: Define `--color-bookmark-badge` tokens or document as intentional

6. **`text-foreground/70` instead of semantic color** (`Library.tsx:141`, confidence: 70)
   - Opacity pattern bypasses `text-muted-foreground` semantic token
   - Unpredictable contrast depending on theme foreground color

7. **Inconsistent sizing pattern** (`Library.tsx:134`, confidence: 65)
   - `w-14 h-9` used where width/height differ (not a `size-*` candidate, just noted)

### Nits

8. **No return type on `findCourseAndLesson`** (`Library.tsx:58-63`, confidence: 60)
9. **Date formatting computed inline** (`Library.tsx:142`, confidence: 55)
10. **Stale RED phase comment in E2E spec** (`story-e03-s07.spec.ts:1-11`, confidence: 50)

## Verdict

**1 Blocker** (uncommitted code), **3 High**, **3 Medium**, **3 Nits** — 10 findings total.
