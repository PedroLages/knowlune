# Code Review: E58-S01 Notifications Page

**Date:** 2026-03-28
**Story:** E58-S01 — Notifications Page
**Reviewer:** Claude Opus 4.6 (automated)

## Files Changed

- `src/app/pages/Notifications.tsx` (new)
- `src/app/components/figma/NotificationCenter.tsx` (unchanged, pre-existing)
- `src/app/routes.tsx` (lazy import + route added)
- `tests/e2e/notifications-page.spec.ts` (new)
- `tests/support/helpers/seed-helpers.ts` (added seedNotifications helper)

## Findings

### MEDIUM: Duplicated icon mapping and relativeTime helper

**Files:** `Notifications.tsx:24-41,65-79` duplicates `NotificationCenter.tsx:23-41,45-59`

The notification icon mapping (`notificationIcons`, `notificationIconColors`, `DEFAULT_ICON`, `DEFAULT_ICON_COLOR`) and `relativeTime()` helper are copy-pasted between NotificationCenter and Notifications page. The story file (Task 1.2, 1.3) acknowledges this as intentional reuse, but a shared module would reduce maintenance burden.

**Recommendation:** Extract to `src/app/components/figma/notification-utils.ts` in a future chore.

### LOW: seedNotifications in seed-helpers.ts is now unused by the test

The `seedNotifications` typed convenience helper was added to `seed-helpers.ts` but the E2E test ended up using inline `page.evaluate()` for browser-relative timestamps (to avoid TTL cleanup). The helper is still useful for future tests.

**Verdict:** Not a problem -- the helper is available for tests that don't need TTL-relative timestamps.

## Architecture

- Route registration follows existing lazy-loading pattern with `SuspensePage` wrapper
- Page component correctly reads from the shared `useNotificationStore` Zustand store
- Store initialization is handled by `NotificationCenter` in the Layout header (correct dependency)
- Filtering uses `useMemo` for performance
- Actions use `useCallback` for stable references

## Security

- No user input rendered as HTML (XSS safe)
- No API calls or data fetched from external sources
- Notification data is local-only (Dexie/IndexedDB)

## Accessibility

- ARIA labels on all interactive elements (filter buttons, mark-as-read, dismiss)
- `aria-pressed` on filter toggle buttons
- `role="list"` and `role="listitem"` for semantic list structure
- `aria-live="polite"` region for screen reader announcements
- Touch targets meet 44px minimum (`min-h-[36px]` on filters, `min-w-[44px]` on action buttons)
- Keyboard navigable (all actions are buttons)

## Test Quality

- 11 E2E tests covering all 7 acceptance criteria
- Tests use browser-relative timestamps to avoid TTL cleanup issues
- Welcome wizard and onboarding overlay properly dismissed in beforeEach
- Cleanup in afterEach prevents cross-test contamination

## Verdict: PASS
