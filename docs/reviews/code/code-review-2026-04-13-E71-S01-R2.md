## Code Review: E71-S01 — Round 2

**Reviewer**: Claude Opus 4.6
**Date**: 2026-04-13
**Scope**: `src/lib/actionSuggestions.ts`, `src/lib/__tests__/actionSuggestions.test.ts`
**Round 1 fixes verified**: All 6 applied correctly (input clamping, URL encoding, safe .get(), test coverage)

### Verdict: PASS

### Story-Related Findings

#### LOW

1. **Non-deterministic sort for equal urgency scores** — `src/lib/actionSuggestions.ts:223`
   Topics with identical urgency scores have undefined sort order. Add a secondary sort key (e.g., `canonicalName`) for deterministic behavior.

2. **Lesson route missing `encodeURIComponent`** — `src/lib/actionSuggestions.ts:140`
   `courseId` and `lessonId` are interpolated raw into the lesson route, unlike flashcard/quiz routes which encode `canonicalName`. Internal IDs are safe today, but inconsistent with the encoding pattern applied elsewhere.

#### NON-ISSUES (from GLM review)

- **canonicalName dedup collision**: Input contract is one entry per canonical topic. Type system and caller responsibility.
- **completionPct NaN/undefined**: Field is typed as `number` (required). TypeScript enforces at compile time.
- **fsrsDecayFactor asymmetric upper bound**: `calculateUrgencyScore` clamps final output to [0,100], making intermediate overflow harmless.

### Pre-Existing Issues: 0
### Known Issues: 0 (KI-057 through KI-063 acknowledged, not re-flagged)
