## External Code Review: E71-S03 — GLM (glm-5.1)

**Model**: GLM (glm-5.1)
**Date**: 2026-04-13
**Story**: E71-S03

### Findings

#### Blockers
*(none)*

#### High Priority
- **[src/stores/useKnowledgeMapStore.ts:389-395] (confidence: 92)**: **Fabricated lesson data with misleading IDs.** The mapping `t.courseIds.map(courseId => ({ lessonId: courseId, ... }))` creates fake lesson objects where `lessonId` equals the `courseId`. This violates the data contract — downstream code in `generateActionSuggestions` that uses `lessonId` will produce incorrect routes/references (e.g., navigating to a course ID as if it were a lesson). The comment says "approximated" but the data shape is actively misleading and will cause real bugs if the CTA links use these IDs.
  Fix: Either pass an empty `lessons: []` array when real lesson data is unavailable, or clearly namespace the fake IDs (e.g., `lessonId: 'placeholder-${courseId}'`) and have `actionSuggestions` skip placeholder lessons. Better yet, gate the lesson-rewatch suggestion on having actual lesson data rather than fabricating it.

- **[src/stores/useKnowledgeMapStore.ts:385-387] (confidence: 88)**: **Inverted trend heuristic.** The trend is computed as: `>14 days → 'declining'`, `>7 days → 'stable'`, else `'improving'`. This means a topic engaged 1 day ago gets trend `'improving'`, while one engaged 30 days ago gets `'declining'` — that may be correct. However, the thresholds feel inverted relative to the comment on recencyScore: `recencyScore: Math.max(0, 100 - t.daysSinceLastEngagement * 2)` gives 0 for anything >50 days. The real issue is that `recencyScore` is 100 for `daysSinceLastEngagement=0` and 0 for 50+ days, but `trend` uses different breakpoints. Topics at 8-14 days are marked `'stable'` despite having `recencyScore` of 72-84 (high). If `generateActionSuggestions` treats `'stable'` trend differently from `'declining'`, topics in the 8-14 day window may be deprioritized despite being at genuine risk. This misalignment could cause the suggestion engine to miss genuinely fading topics.
  Fix: Align the trend breakpoints with the recencyScore decay curve, or derive trend directly from the recencyScore (e.g., `trend = recencyScore > 80 ? 'improving' : recencyScore > 40 ? 'stable' : 'declining'`).

#### Medium
- **[tests/e2e/story-e71-s03.spec.ts:216-226] (confidence: 75)**: **Flaky responsive test — viewport resize without reload may not re-trigger store computation.** The test sets desktop viewport, loads the page and asserts the panel, then sets mobile viewport and loads the page again with `goto`. This is fine on its own, but the test doesn't assert that the panel is *inline* (mobile layout) vs *sidebar* (desktop layout) — it just checks visibility in both cases, which doesn't actually validate the responsive behavior described in the AC. The test could pass even if both instances rendered simultaneously (which they do — both `{isMobile && ...}` and `{!isMobile && ...}` exist in the DOM, just toggled via CSS media query).
  Fix: Add assertions that verify panel placement (e.g., check that on desktop the panel is inside the sidebar column, and on mobile it's above the treemap). Alternatively, assert that only one panel instance is visible at each breakpoint by checking the parent container relationship.

- **[src/app/pages/KnowledgeMap.tsx:147] (confidence: 65)**: **Duplicate component rendering.** `<SuggestedActionsPanel>` is rendered twice in the component tree — once for mobile, once for desktop — controlled by the `isMobile` boolean. If `isMobile` doesn't perfectly match the CSS breakpoint used by `lg:w-80` (Tailwind's `lg` is 1024px), there could be a viewport range where neither instance renders or both are visible simultaneously. This is only a visual glitch but worth verifying the breakpoint alignment.
  Fix: Ensure the `isMobile` state uses the same breakpoint as Tailwind's `lg` prefix (1024px). Consider using `window.matchMedia('(max-width: 1023px)')` or a shared constant.

#### Nits
- **[src/stores/useKnowledgeMapStore.ts:180] (confidence: 90)**: **Stale data wiped on error path.** When `computeScores` catches an error, it resets `suggestions: []` along with all other state. This means a transient error (e.g., IndexedDB hiccup) wipes the user's visible suggestions with no recovery path until the next successful computation. Consider preserving last-known-good suggestions on error, similar to how some stores cache previous results.

---
Issues found: 5 | Blockers: 0 | High: 2 | Medium: 2 | Nits: 1
