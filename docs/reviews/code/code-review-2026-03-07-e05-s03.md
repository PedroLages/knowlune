# Code Review: E05-S03 — Study Goals & Weekly Adherence (Re-run)

**Review Date**: 2026-03-07
**Reviewed By**: Claude Code (code-review agent)

## What Works Well

1. **Clean separation of concerns**: Pure-function library at `src/lib/studyGoals.ts` keeps all business logic testable outside React.
2. **Event-driven refresh via CustomEvent**: `study-goals-updated` and `study-log-updated` window events with proper cleanup.
3. **Comprehensive unit test coverage**: CRUD, daily/weekly progress, adherence, edge cases, entry type filtering, fixed-date approach avoids flaky day-boundary issues.

## Previous Blockers — Status

- Division by zero in `buildProgress`: Fixed with `target <= 0` guard ✓
- Edit button 16px touch target: Fixed with min-w/h-[44px] ✓
- ProgressRing SVG missing aria-hidden: Fixed ✓
- Non-null assertions: Replaced with `progress && adherence` guard ✓

## Findings

### Blockers

1. **Working tree changes not committed** (confidence: 99)
   - All review fixes exist only in working tree, not committed.
   - Fix: Commit all working tree changes before merging.

### High Priority

2. **Inconsistent "week" definition** (`studyGoals.ts:76-91 vs 134-155`, confidence: 82)
   - `computeWeeklyProgress` uses Monday-based calendar week; `computeWeeklyAdherence` uses rolling 7-day window.
   - Fix: Align both to the same week boundary.

3. **`handleSave` allows saving invalid targets** (`StudyGoalConfigDialog.tsx:46-57`, confidence: 85)
   - Does not catch `target === "0"` or negative values.
   - Fix: Add `const numTarget = Number(target); if (!numTarget || numTarget <= 0 || !Number.isFinite(numTarget)) return`.

4. **`getStudyLog()` trusts localStorage blindly** (`StudyGoalsWidget.tsx:9-16`, confidence: 78)
   - No validation that parsed result is an array.
   - Fix: `const parsed = JSON.parse(raw); return Array.isArray(parsed) ? parsed : []`.

### Medium

5. **ProgressRing uses string interpolation vs `cn()`** (`ProgressRing.tsx:19`, confidence: 75)
   - Fix: Use `cn()` for class merging.

6. **Custom `<input>` instead of shadcn `<Input>`** (`StudyGoalConfigDialog.tsx:150-158`, confidence: 72)

7. **`isToday` midnight boundary risk** (`studyGoals.ts:72-73`, confidence: 68)
   - `new Date()` called per entry in filter loop.

### Nits

8. `text-success` contrast verification needed (confidence: 60)
9. Target icon in EmptyState missing `aria-hidden="true"` (confidence: 55)
10. No user feedback on save validation failure (confidence: 55)

## Summary

Issues: 10 | Blockers: 1 | High: 3 | Medium: 3 | Nits: 3
