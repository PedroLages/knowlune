# Code Review (v2): E03-S01 — Markdown Note Editor with Autosave

**Date**: 2026-02-23
**Type**: Re-review after blocker fixes
**Reviewer**: Code Review Agent (Adversarial)
**Files reviewed**: NoteEditor.tsx, LessonPlayer.tsx, story-3-1.spec.ts, package.json

## Previous Fix Verification

| Fix | Status |
|-----|--------|
| B1: `video://` protocol registration | VERIFIED FIXED — `protocols: ['video']` in StarterKit link config |
| B2: `onVideoSeek` stale closure | VERIFIED FIXED — `onVideoSeekRef` latest-ref pattern added |
| H1: Unmount cleanup editor null | VERIFIED FIXED — `editorRef` tracks latest editor instance |
| H2: `handleNoteChange` double write | VERIFIED FIXED — `setNoteText(value)` removed |
| H3: Missing AC4 click-to-seek E2E | VERIFIED FIXED — test added (with caveat, see H1 below) |

## Findings

### High Priority
- **H1**: AC4 click-to-seek test only verifies video player is visible, not that seek actually occurred
- **H2**: `handleNoteChange` not wrapped in `useCallback` — causes ~240 unnecessary NoteEditor re-renders/min during playback

### Medium
- **M1**: `@tiptap/extension-link` is redundant direct dependency in package.json (StarterKit includes it)
- **M2**: `h-4 w-4` should use Tailwind v4 `size-4` shorthand
- **M3**: `setContent` empty-state comparison doesn't account for Tiptap's `<p></p>` empty representation

### Nits
- Toolbar divider repeated — extract `ToolbarDivider` component
- `formatTimestamp` still duplicated across 5+ files
- RED PHASE comment in spec file is stale (tests pass)
- `cursor-pointer` on `<button>` is redundant

## AC Coverage

| AC | Verdict |
|----|---------|
| 1 | Partial — keyboard shortcuts untested |
| 2 | Partial — max-wait and MiniSearch untested |
| 3 | Pass |
| 4 | Gap — seek test doesn't verify actual seek |

## Verdict
**No blockers.** All previous blockers verified fixed. 2 high-priority, 3 medium, 4 nits.
Issues found: 9 | Blockers: 0 | High: 2 | Medium: 3 | Nits: 4
