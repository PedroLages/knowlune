# Code Review: E05-S04 — Study History Calendar

**Date**: 2026-03-07 (re-run)
**Reviewer**: Code Review Agent

## Summary

Issues found: 10 | Blockers: 1 | High: 4 | Medium: 5 | Nits: 3

## Findings

### Blockers

- **Uncommitted fixes pattern (confidence: 95)**: Committed branch (`45f69e9`) ships code with 4 known issues only fixed in working tree: (1) `parentElement!` non-null assertion crash risk; (2) hardcoded color classes bypass theme tokens; (3) DayButton defined inline causing remounts; (4) nav button `size-7` (28px) vs `size-11` (44px). Unit test file `src/lib/__tests__/studyCalendar.test.ts` also untracked. Fix: Commit all working-tree changes before merge.

### High Priority

- **Inline styles for popover positioning (confidence: 88)**: `handleDayClick` uses `anchorRef.current.style.position/left/top`. Violates Tailwind-only convention and becomes stale on resize. Fix: Use Radix `PopoverTrigger` per day button or add resize listener.

- **Performance: full localStorage parse on every navigation (confidence: 85)**: `getMonthStudyData()` calls `getStudyLog()` — full JSON parse of entire study log on every month change and `study-log-updated` event. Fix: Cache results or debounce handler.

- **Empty heading in popover (confidence: 82)**: `dateHeading` is undefined when `selectedDay` is null, rendering empty `<h4>`. Fix: Add fallback or conditional render.

- **Weak E2E assertions for AC3 (confidence: 80)**: Committed test only checks `/course/i` regex — doesn't verify action type or timestamp. Fix: Commit working-tree E2E improvements.

### Medium

- **Eager `courseNameMap` at module load (confidence: 78)**
- **Unnecessary Date construction in binning loop (confidence: 75)**
- **No `prefers-reduced-motion` guard (confidence: 75)**
- **Array index keys for session list items (confidence: 72)**
- **DayPicker `components` object not memoized (confidence: 70)**

### Nits

- Unused `modifiers` destructure in `DayButton` (confidence: 65)
- `<h2>` "Study History" missing `font-semibold` (confidence: 60)
- Inline `makeStudyEntry` factory in E2E (confidence: 55)
