# Code Review: E21-S03 Pomodoro Focus Timer

**Date:** 2026-03-24
**Reviewer:** Claude Opus 4.6 (automated)
**Story:** E21-S03 — Pomodoro Focus Timer
**Branch:** feature/e21-s03-pomodoro-focus-timer

## Summary

The story implements a Pomodoro focus timer integrated into the lesson player. The implementation is well-structured with clean separation of concerns: a drift-free timer hook (`usePomodoroTimer`), a Web Audio chime module (`pomodoroAudio`), a localStorage preferences module (`pomodoroPreferences`), and a popover UI component (`PomodoroTimer`).

## Files Reviewed

- `src/hooks/usePomodoroTimer.ts` — Timer state machine with wall-clock anchoring
- `src/app/components/figma/PomodoroTimer.tsx` — Popover UI component
- `src/lib/pomodoroAudio.ts` — Web Audio API chime
- `src/lib/pomodoroPreferences.ts` — localStorage persistence
- `src/app/pages/LessonPlayer.tsx` — Integration point
- `tests/e2e/regression/e21-s03-pomodoro-timer.spec.ts` — E2E tests

## Findings

### MEDIUM: No unit tests for usePomodoroTimer hook (story-related)

The story Task 8 explicitly calls for unit tests: "Write unit tests for `usePomodoroTimer` hook (AC: 2,3,5)". No unit test file exists. The hook contains complex state machine logic (phase transitions, auto-start, drift-free timing) that would benefit from isolated unit testing beyond E2E coverage.

**File:** `src/hooks/usePomodoroTimer.ts`

### MEDIUM: Hard waits in E2E tests without justification comments (story-related)

Three `waitForTimeout()` calls at lines 67, 74, 79 of the spec file trigger the `test-patterns/no-hard-waits` ESLint warning. These are used in the pause/resume test to wait for the timer to tick. While understandable for real-time timer testing, each should have a justification comment to suppress the lint warning, or ideally use clock mocking to make the test deterministic.

**File:** `tests/e2e/regression/e21-s03-pomodoro-timer.spec.ts:67,74,79`

### LOW: Lessons learned section is empty (story-related)

The story file at `docs/implementation-artifacts/21-3-pomodoro-focus-timer.md` has placeholder text in the "Challenges and Lessons Learned" section. The lessons learned gate requires this to be populated before review passes.

**File:** `docs/implementation-artifacts/21-3-pomodoro-focus-timer.md:109`

### LOW: Pre-review checklist items not checked off (story-related)

All checklist items in the story file remain unchecked (`- [ ]`). These should be verified and marked as done.

**File:** `docs/implementation-artifacts/21-3-pomodoro-focus-timer.md:85-93`

### LOW: Task list items not checked off (story-related)

All 8 task items in the story file remain unchecked despite implementation being complete.

**File:** `docs/implementation-artifacts/21-3-pomodoro-focus-timer.md:60-67`

## Architecture Assessment

**Positive patterns observed:**
- Wall-clock anchoring pattern (same as `useQuizTimer`) prevents drift on tab-switch
- Proper visibility change handler for tab-return accuracy
- Ref-based callback pattern avoids stale closures in setInterval
- Clean separation: hook (logic) / component (UI) / audio (side-effect) / preferences (persistence)
- Correct useEffect cleanup for interval and event listener
- Audio uses Web Audio API (no file dependency, offline-capable)
- Graceful degradation: audio failures don't block timer, localStorage failures fall back to defaults
- All buttons use `variant="brand"` per design token guidelines
- Proper ARIA: `role="timer"`, `aria-live="polite"`, `aria-label` on controls
- `tabular-nums` for countdown prevents layout shift

**No security concerns identified.**
**No silent failure patterns detected** (catch blocks have appropriate comments).

## Verdict

ISSUES FOUND — 2 MEDIUM, 3 LOW issues, all story-related. No blockers.
