# Code Review — E59-S08 (Round 2)

**Date:** 2026-03-30
**Story:** E59-S08 — E2E Tests and Test Factory Updates
**Branch:** `feature/e59-s08-e2e-tests-and-test-factory-updates`
**Round:** 2 (re-review after Round 1 fixes)

## Round 1 Fixes Verified

All 4 issues from Round 1 have been addressed:

1. **[FIXED]** Clock freezing added to `story-e59-s08.spec.ts` — `page.clock.install({ time: new Date(FIXED_DATE) })` in `beforeEach`
2. **[FIXED]** FSRS curve calibration — `createDueFlashcard` stability/elapsed_days tuned for proper scheduling
3. **[FIXED]** Onboarding dismissal in `story-e11-s05.spec.ts` — localStorage set via `addInitScript` before navigation
4. **[FIXED]** Non-deterministic ordering — `story-e11-s05.spec.ts` AC3 finds rated record by `reps > 2` instead of fixed noteId

## New Findings (Round 2)

### MEDIUM: ESLint error — `Date.now()` in regression test

**File:** `tests/e2e/regression/story-e11-s05.spec.ts:232`
**Severity:** MEDIUM

Line 232 uses `Date.now()` in a Node.js assertion context (not inside `page.evaluate`). The ESLint rule `test-patterns/deterministic-time` flags this as an ERROR. While semantically correct here (browser clock is not frozen, FSRS scheduler uses real time), it violates the project's deterministic time convention and triggers a lint error.

**Fix:** Add eslint-disable comment with justification:
```typescript
// eslint-disable-next-line test-patterns/deterministic-time -- comparing against real browser time (no clock freeze in this spec)
expect(new Date(persisted!.due).getTime()).toBeGreaterThan(Date.now())
```

## Pre-Check Results

| Gate | Result |
|------|--------|
| Build | PASS |
| Lint | 1 ERROR (Date.now() above), 24 warnings (pre-existing) |
| TypeScript | 5 errors in `schema.test.ts` (pre-existing, not in branch diff) |
| Prettier | PASS |
| E2E story tests | 8/8 PASS |
| E2E regression tests | 20/20 PASS (1 skipped, pre-existing) |
| Unit tests | Pre-existing failures in `isPremium.test.ts`, `AtRiskBadge.test.tsx`, `VideoReorderList.test.tsx` (not in branch diff) |

## Code Quality Assessment

- Factory design is clean with proper FSRS defaults and convenience variants
- Barrel export in `factories/index.ts` follows established pattern
- `seedReviewRecords` and `seedFlashcards` helpers follow the existing `seedIndexedDBStore` pattern
- Test structure is well-organized with clear AC mapping
- Proper cleanup in `afterEach` with `.catch()` guards

## Verdict

**PASS with 1 MEDIUM** — The `Date.now()` lint error requires an eslint-disable comment. All Round 1 issues confirmed fixed. No regressions introduced.
