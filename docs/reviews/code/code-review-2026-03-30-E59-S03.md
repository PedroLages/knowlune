# Code Review: E59-S03 — Dexie v31 Schema Migration (SM-2 to FSRS) — Round 2

**Date:** 2026-03-30
**Reviewer:** Claude Opus 4.6 (automated)
**Branch:** `feature/e59-s03-dexie-v29-schema-migration`
**Commits:** 4 (feat + style fix + docs + review fixes)
**Round:** 2 (Round 1 found 2 MEDIUM + 1 LOW, all fixed)

## Files Changed

| File | Type | Lines |
|------|------|-------|
| `src/db/schema.ts` | Migration logic | +114 |
| `src/db/checkpoint.ts` | Checkpoint bump v30→v31 | +8/-7 |
| `src/db/__tests__/migration-v31-fsrs.test.ts` | New test file | +451 |
| `src/db/__tests__/schema-checkpoint.test.ts` | Version bump | +2/-2 |
| `src/db/__tests__/schema.test.ts` | FSRS field updates | +37/-30 |
| `docs/implementation-artifacts/stories/E59-S03.md` | Story doc | new |
| `docs/implementation-artifacts/sprint-status.yaml` | Tracking | updated |

## Round 1 Findings — Verification

### MEDIUM-1: Non-deterministic `elapsed_days` — FIXED
`calcElapsedDays()` previously called `new Date()` per record. Now uses `migrationNow` captured once at upgrade callback entry (line 1074). All records in a single migration run get consistent elapsed_days.

### MEDIUM-2: Non-deterministic fallback `due` dates — FIXED
Both flashcard (line 1131) and reviewRecord (line 1160) fallbacks now use `migrationNowIso` (captured once, line 1075) instead of per-record `new Date().toISOString()`.

### LOW-3: Redundant truthiness check — FIXED
`if (!reviewCount || reviewCount === 0)` simplified to `if (!reviewCount)` (line 1095).

## Round 2 Analysis — No New Issues

### Architecture Assessment

- **Migration pattern**: Correct Dexie `.version(31).stores().upgrade()` chain
- **Checkpoint sync**: `checkpoint.ts` correctly updated to v31 with FSRS indexes
- **Data safety**: SM-2 fields deleted only after FSRS fields written (same `modify` callback, atomic per-record)
- **Parallel execution**: `Promise.all([flashcardsMigration, reviewRecordsMigration])` correct for independent tables
- **Scope discipline**: Only schema.ts/checkpoint.ts changed; store updates deferred to E59-S04
- **Deterministic migration**: Single `migrationNow` timestamp shared across all records
- **Edge cases handled**: Missing fields default safely, extreme easeFactor clamped to [1.3, 2.5], empty tables no-op

### Mapping Correctness

| SM-2 Field | FSRS Field | Conversion | Correct? |
|-----------|-----------|------------|----------|
| `easeFactor` (1.3-2.5) | `difficulty` (0-10) | Linear inverse map, clamped | Yes |
| `interval` (days) | `stability` (days) | Direct copy, `max(0, ...)` | Yes |
| `reviewCount` | `reps` | Direct copy | Yes |
| `nextReviewAt` | `due` | Rename, fallback chain | Yes |
| `reviewedAt` | `last_review` | Rename | Yes |
| (new) | `lapses` | Default 0 | Yes (SM-2 lacks this) |
| (new) | `state` | Derived from reviewCount + interval | Yes |
| (new) | `elapsed_days` | Days since last review | Yes |
| (new) | `scheduled_days` | Same as interval | Yes |

### Security Assessment

No security concerns. Client-side IndexedDB migration with no network calls, no user input, no secrets.

## Pre-Checks

| Gate | Status |
|------|--------|
| Build | PASS |
| Lint | PASS (0 errors; 25 pre-existing warnings in unrelated E2E files) |
| Type check | 19 pre-existing errors in `useFlashcardStore.ts` and `useReviewStore.ts` (expected — store updates deferred to E59-S04) |
| Prettier | PASS (1 cosmetic line-wrap fix in schema.ts applied) |
| Unit tests | PASS (70/70 across 3 test files) |

## Verdict: PASS

All Round 1 findings resolved. No new issues introduced. Migration logic is correct, well-tested, and properly scoped.
