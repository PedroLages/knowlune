# Code Review: E59-S02 — FSRS Algorithm Wrapper

**Date:** 2026-03-29
**Reviewer:** Claude (automated)
**Story:** E59-S02 — FSRS Algorithm Wrapper
**Branch:** `feature/e59-s02-fsrs-algorithm-wrapper`
**Files Changed:** `src/lib/spacedRepetition.ts` (132 insertions, 93 deletions)

## Summary

Rewrites `spacedRepetition.ts` from SM-2 to FSRS (Free Spaced Repetition Scheduler) using the `ts-fsrs` library. Clean single-gateway pattern with proper type conversion between app-level types and ts-fsrs types.

## Findings

### PASS — No Story-Related Issues

The implementation is clean and well-structured:

1. **Single gateway pattern enforced** — Only `spacedRepetition.ts` imports from `ts-fsrs`. Confirmed via codebase grep.

2. **Type safety** — `FSRSSchedulingResult` and `FSRSCardState` interfaces align exactly with the FSRS fields defined in `data/types.ts` (`ReviewRecord` and `Flashcard`).

3. **Edge case handling** — New cards (null input) correctly use `createEmptyCard(now)` from ts-fsrs. Invalid dates in `predictRetention` return 0 (line 148). Negative elapsed days return 100% retention. Zero stability returns 0%.

4. **Test determinism** — Exports `fsrsTest` with `enable_fuzz: false` for deterministic unit test assertions.

5. **No side effects** — Pure functions only, no database access, no state mutations.

6. **Clean rating conversion** — `RATING_MAP` handles all 4 ratings including `again` (which was missing from old SM-2 `QUALITY_MAP`).

7. **Date handling** — ISO string <-> Date conversion happens at the boundary only (lines 100, 108, 123-126, 147, 169).

### LOW — Formatting Issue (Auto-Fixed)

Prettier formatting inconsistency detected and auto-fixed (trailing commas in function parameters, ternary formatting).

### Pre-Existing Type Errors (152 errors in 15 files)

All type errors are in consumer files that still reference SM-2 fields (`interval`, `easeFactor`, `nextReviewAt`, `reviewedAt`, `reviewCount`). These are expected and documented in the story file — consumer migration is planned for E59-S04 and E59-S05. Note: main branch has 160 type errors, so this story actually reduced the count by 8.

### Pre-Existing Unit Test Failures

`spacedRepetition.test.ts` has 20/23 tests failing — they test SM-2 behavior. Test rewrite is planned for E59-S07. This is documented and expected.

## Verdict

**PASS** — Implementation is correct, well-documented, and follows all architectural patterns. No blockers or issues in story-changed files.
