# Code Review: E91-S04 Mini Player (Round 2)

**Date:** 2026-03-30
**Story:** E91-S04 — Mini Player (Picture-in-Picture style)
**Reviewer:** Claude Opus 4.6 (automated)
**Round:** 2 (re-review after fixes)

## Round 1 Issues — Verification

| # | Severity | Issue | Status |
|---|----------|-------|--------|
| 1 | BLOCKER | Missing `focusTab` state after mini-player state additions | FIXED — `focusTab` state preserved, mini-player state reset added to lessonId effect |
| 2 | HIGH | Missing E2E tests | FIXED — 4 E2E tests covering AC1-AC4, all passing |
| 3 | MEDIUM | Dual audio from mini-player + main player | FIXED — `muted` attribute on mini-player `<video>` element |

## Round 2 Findings

### Story-Related Issues

**None found.** Implementation is clean:

- MiniPlayer component is well-structured with proper ARIA attributes
- IntersectionObserver cleanup handled correctly
- Animation mount/unmount pattern avoids layout thrash
- `silent-catch-ok` comments satisfy ESLint error-handling rule
- forwardRef pattern correctly forwards VideoPlayerHandle
- Lesson change resets all mini-player state
- E2E tests acknowledge OPFS limitation and test conditional rendering guards

### Pre-Existing Issues

- 3 ESLint parsing errors in `scripts/get-smoke-specs*.js` (shell scripts with wrong extension)
- 5 TypeScript errors in `src/db/__tests__/schema.test.ts` (CardState type mismatch)
- Unit test coverage at 69.98% vs 70% threshold (pre-existing)
- 8 Prettier formatting warnings in unrelated files

### Architecture Notes

- Clean separation: MiniPlayer is a pure presentational component, all state managed by UnifiedLessonPlayer
- IntersectionObserver threshold of 0.1 is appropriate for "scrolled out of view" detection
- Time sync drift threshold of 1 second prevents excessive seeking
- `bottom-16` on mobile avoids bottom nav overlap, `sm:bottom-4` on desktop

## Verdict

**PASS** — All Round 1 issues resolved. No new story-related issues found.
