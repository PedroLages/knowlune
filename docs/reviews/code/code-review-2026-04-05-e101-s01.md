# Code Review: E101-S01 — AudiobookshelfService & Data Foundation

**Reviewer**: Claude Opus (code-review agent)
**Date**: 2026-04-05
**Round**: 4 (clean verification pass)
**Story**: E101-S01

## Summary

Clean implementation. All Round 3 findings have been resolved. The AudiobookshelfService follows the established OpdsService pattern exactly: pure functions, discriminated union returns, AbortController timeouts, and user-friendly error messages. Schema migration, checkpoint, types, and unit tests are all correctly implemented.

## Round 3 Findings — Resolution Status

| # | Severity | Finding | Status |
|---|----------|---------|--------|
| 1 | HIGH | Timeout test didn't test mechanism | FIXED — now uses `advanceTimersByTimeAsync` with abort signal listener |
| 2 | MEDIUM | SyntaxError not handled in catch block | FIXED — `SyntaxError` check added at line 83 |
| 3 | MEDIUM | No trailing-slash normalization | FIXED — `baseUrl.replace(/\/+$/, '')` at line 60 |
| 4 | MEDIUM | Prettier formatting issues | FIXED — Prettier check passes |
| 5 | NIT | Content-Type header sent on GET requests | FIXED — `hasBody` guard at line 44 |

## Round 4 Review Passes

### Pass 1 — Blind Hunter (Adversarial)
- Reviewed `git diff main...HEAD` for all 8 changed files
- No null dereferences, race conditions, or security issues found
- Error handling is comprehensive with exact error message strings matching AC

### Pass 2 — Edge Case Hunter (Systematic)
- All async paths properly handle failure (TypeError, DOMException, SyntaxError, generic)
- `clearTimeout` called in both happy path (line 62) and catch block (line 78) — no timer leaks
- URL parameters properly encoded via `encodeURIComponent` in all path-building functions
- Non-null assertion `options!.body` at line 57 is safe — guarded by `hasBody` check on line 44
- Trailing-slash normalization applied in `absApiFetch`, `getStreamUrl`, and `getCoverUrl`
- `AbsItem` defined as type alias to `AbsLibraryItem` — correct per AC "extended in E102+ if needed"

### Pass 2b — AI Code Smell Check
- No placeholder/template text
- No TODO/FIXME without tracking reference
- No dead code or unused imports
- No over-engineering

### Pass 3 — Story Compliance
- All AC verified against implementation
- Error messages match exact strings specified in Dev Notes
- `getStreamUrl` uses `?token=` query parameter per Architecture Decision 5
- Schema bump 39 -> 40 with correct table indices
- Checkpoint version, schema, and test all updated consistently
- `Book` type extended with optional `absServerId` and `absItemId` fields
- All `Abs*` response types properly defined
- 24 unit tests covering success, auth failure, timeout, CORS, server error, malformed JSON, URL encoding

## Findings

### Story-Related

**None.** All prior findings resolved. Code is clean.

### Cross-Model Review Notes

**GLM-5.1** (see `docs/reviews/code/glm-review-2026-04-05-E101-S01.md`):

1. **BLOCKER -> Downgraded to NIT**: `clearTimeout` before `response.json()` — matches established OpdsService pattern. `AbortController` signal only aborts `fetch()`, not `.json()` parsing. Not actionable.
2. **MEDIUM -> NOT ACTIONABLE**: API key in query param — intentional architectural decision (Decision 5). HTML5 `<audio>` cannot send custom headers.
3. **NIT -> ACKNOWLEDGED**: Plaintext API key tracking — comment already present noting local-first-acceptable with cloud-sync encryption tracking.

**OpenAI Codex**: Failed with exit code 2 (API error). Not available.

## Verdict

**PASS** — Zero actionable findings.

---
Issues found: 0 | Blockers: 0 | High: 0 | Medium: 0 | Nits: 0
