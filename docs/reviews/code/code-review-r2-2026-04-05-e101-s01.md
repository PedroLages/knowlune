## Code Review (Round 2): E101-S01 — AudiobookshelfService & Data Foundation

### Round 1 Fixes Verified

All 5 findings from round 1 have been addressed:

1. **Timeout test (was HIGH)**: FIXED. Test now uses `vi.advanceTimersByTimeAsync(10_001)` with a never-resolving promise + abort signal listener. Properly tests the `setTimeout -> controller.abort()` integration.
2. **SyntaxError handling (was MEDIUM)**: FIXED. Catch block now checks `err instanceof SyntaxError` and returns "Server returned an invalid response. Check the URL." (line 83-85).
3. **Trailing-slash normalization (was MEDIUM)**: FIXED. `absApiFetch` now normalizes base URL with `baseUrl.replace(/\/+$/, '')` (line 60).
4. **Content-Type on GET (was NIT)**: FIXED. `Content-Type: application/json` only added when `hasBody` is true (lines 44-48).
5. **Prettier formatting (was MEDIUM)**: FIXED. All files pass `prettier --check`.

### GLM Round 1 Fixes Verified

1. **Timeout test (was BLOCKER)**: FIXED (same as above).
2. **SyntaxError handling (was BLOCKER)**: FIXED (same as above).
3. **getCoverUrl missing auth (was HIGH)**: NON-ISSUE. ABS serves covers publicly by default. Story spec explicitly lists `getCoverUrl` as a non-fetch URL builder without auth. The actual authenticated API calls go through `absApiFetch`.
4. **Response.ok in test environment (was NIT)**: NON-ISSUE. Vitest uses Node.js native `Response` which correctly sets `ok` based on status code. All 28 tests pass.

### New Findings (Round 2)

#### Blockers

(none)

#### High Priority

(none)

#### Medium

(none)

#### Low

- **[src/services/AudiobookshelfService.ts:187] Inconsistent `encodeURIComponent` on path parameters (confidence: 70)**: `searchLibrary` interpolates `libraryId` directly into the URL path without encoding, while `fetchLibraryItems` (line 139) and `fetchItem` (line 152) use `encodeURIComponent`. Similarly, `fetchProgress` (line 201) and `updateProgress` (line 215) don't encode `itemId`. Since ABS IDs are UUIDs (safe characters), this is cosmetic but inconsistent.

- **[src/services/AudiobookshelfService.ts:163,171] Pure URL builders don't normalize trailing slashes (confidence: 60)**: `getStreamUrl` and `getCoverUrl` don't normalize `baseUrl` trailing slashes, while `absApiFetch` does (line 60). A user entering `http://server:13378/` would get double slashes in cover/stream URLs but correct URLs in API calls. Low risk since these functions are called by the store layer which should normalize URLs before storage.

#### Nits

(none)

### Verdict

**PASS** — All round 1 blockers and high-priority issues resolved. Remaining findings are LOW severity defensive improvements that can be addressed in a future chore commit.

---
Issues found: 2 | Blockers: 0 | High: 0 | Medium: 0 | Low: 2 | Nits: 0
