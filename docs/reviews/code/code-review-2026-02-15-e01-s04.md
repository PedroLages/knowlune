# Code Review: E01-S04 — Manage Course Status

**Review Date**: 2026-02-15 (re-review)
**Reviewed By**: Claude Code (code-review agent, Opus)
**Story**: E01-S04 "Manage Course Status"
**Branch**: `feature/e01-s04-manage-course-status`

## Executive Summary

Re-review validates that all blockers from the previous review have been fixed: gray-400 color is correct, `updateCourseStatus` store action has tests, status filtering tests exist in Courses.test.tsx, and status badge/dropdown tests exist in ImportedCourseCard.test.tsx. The `getAllTags` re-render issue from S03 has also been addressed. No blockers remain. Four high-priority findings relate to test coverage gaps.

## What Works Well

1. **Dexie v2 migration**: Correctly adds `status` index and backfills existing records with `'active'` via `upgrade()`.
2. **getAllTags re-render fix**: `ImportedCourseCard` now receives `allTags` as a prop from `Courses.tsx`, eliminating the re-render cascade.
3. **Optimistic update pattern**: `updateCourseStatus` follows the same optimistic-update-with-rollback pattern as `updateCourseTags`.
4. **Test coverage**: 21 tests for ImportedCourseCard (including 7 for status), 12 for Courses page (6 for status filtering), 3 for updateCourseStatus store action.

## Findings

### Blockers

None.

### High Priority

1. **`updateCourseTags` has zero test coverage** — While `updateCourseStatus` tests were added (fixing previous finding), `updateCourseTags` still has no dedicated tests. Both actions follow the identical optimistic-update-with-rollback pattern, and the rollback path is untested.
   - Location: `src/stores/__tests__/useCourseImportStore.test.ts`
   - Fix: Add `describe('updateCourseTags')` with: optimistic update, persistence, rollback on failure, no-op for nonexistent course.

2. **`updateCourseStatus` rollback/error path is untested** — The store has explicit rollback logic (lines 152-159 of useCourseImportStore.ts) that restores `oldStatus` and sets `importError` on failure. This path is completely untested.
   - Location: `src/stores/__tests__/useCourseImportStore.test.ts:174-212`
   - Fix: Mock `db.importedCourses.update` to reject, assert status reverts and `importError` is set.

3. **No dedicated StatusFilter unit tests** — The component is tested indirectly via Courses.test.tsx but has no `__tests__/StatusFilter.test.tsx`. StatusFilter has its own toggle logic, accessibility contract, and conditional rendering.
   - Fix: Create `src/app/components/figma/__tests__/StatusFilter.test.tsx` covering toggle, multi-select, clear button, aria-pressed, callbacks.

4. **Inline `makeCourse` factories instead of shared factory** — Both test files define their own `makeCourse()` with inline defaults instead of using the shared factory at `tests/support/fixtures/factories/`.
   - Location: `src/app/pages/__tests__/Courses.test.tsx:8-31`, `src/stores/__tests__/useCourseImportStore.test.ts:9-22`
   - Fix: Import from shared factory and use overrides.

### Medium

5. **Inconsistent `h-N w-N` vs `size-N` Tailwind v4 shorthand** — Courses.tsx uses `h-4 w-4` (6 instances) while StatusFilter and ImportedCourseCard correctly use `size-N`.
   - Location: `src/app/pages/Courses.tsx:118,123,133,179,191,196`
   - Fix: Replace with `size-N` shorthand throughout.

6. **TagBadgeList uses string interpolation instead of `cn()`** — Line 18 uses template literal for className. Convention is `cn()`.
   - Location: `src/app/components/figma/TagBadgeList.tsx:18`
   - Fix: `className={cn('flex flex-wrap gap-1.5', className)}`

7. **TopicFilter uses string interpolation instead of `cn()`** — Lines 43-46 use ternary in raw string. StatusFilter (same component family) correctly uses `cn()`.
   - Location: `src/app/components/figma/TopicFilter.tsx:43-46`

8. **Combined filter test doesn't prove AND-semantics** — The test selects Active + alpha, but Paused Course was already excluded by status alone. Need a course matching status but not topic to prove AND-combination.
   - Location: `src/app/pages/__tests__/Courses.test.tsx:213-233`

### Nits

9. **Unnecessary `getAllTags` in useMemo deps** — `getAllTags` is referentially stable from Zustand; it adds noise to the dependency array.
   - Location: `src/app/pages/Courses.tsx:62`

10. **Type assertion on Object.entries** — Could use a typed constant array for status keys instead of the cast.
    - Location: `src/app/components/figma/ImportedCourseCard.tsx:100`

## AC Coverage

| AC# | Description | Coverage | Verdict |
|-----|-------------|----------|---------|
| 1a | Status persisted in IndexedDB | Store tests: optimistic + persistence | Pass |
| 1b | Visual badge on course card | ImportedCourseCard tests: badge per status | Pass |
| 1c | Correct colors (blue-600, green-600, gray-400) | Code verified: StatusFilter:28, ImportedCourseCard:33 | Pass |
| 2a | Status filter on Courses page | Courses tests: 6 status filter tests | Pass |
| 2b | Combines with topic filters | Courses test exists but is weak (Finding 8) | Weak |
| 2c | Active filter state indicated | aria-pressed tested, variant toggle in code | Pass |
| 3 | New imports default to Active | courseImport.ts:168 sets `status: 'active'` | Pass |

## Summary

**Issues found: 10** | Blockers: 0 | High: 4 | Medium: 4 | Nits: 2
