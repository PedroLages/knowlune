## Code Review: E111-S02 — Skip Silence and Speed Memory

### What Works Well

1. **Module-level singleton pattern for Web Audio API**: The `_audioCtx`, `_mediaSource`, and `_analyser` guards in `useSilenceDetection.ts` correctly handle the `createMediaElementSource()` one-call constraint. The decision to keep the AnalyserNode connected (rather than disconnect/reconnect) avoids audio glitches. This is a well-documented, deliberate architecture choice.

2. **Optimistic update with rollback in `updateBookPlaybackSpeed`**: Follows the exact same pattern as `updateBookPosition` and `updateBookMetadata` — optimistic Zustand update, Dexie persist, rollback on failure with toast feedback. Consistent and correct.

3. **Per-book speed restore logic in `useAudiobookPrefsEffects`**: The `defaultSpeedAppliedForBookRef` guard prevents stale store rates from a previous book incorrectly blocking the restored speed. Reading from `getState()` inside the callback avoids stale closure on `book.playbackSpeed`. The fallback chain (`book.playbackSpeed` -> `defaultSpeed`) is clean.

### Findings

#### Blockers

None.

#### High Priority

None.

#### Medium

None.

#### Nits

- **[Recurring] [Nit]** `SilenceSkipIndicator.tsx:50`, `SkipSilenceActiveIndicator.tsx:22`, `SpeedControl.tsx:58` (confidence: 75) **[Maintainability]**: String interpolation for conditional `className` instead of `cn()`. Affects 3 new/modified components. Fix: Use `cn()` from `@/app/components/ui/utils` for class merging. ~5 min.

### Recommendations

The implementation is clean. The only finding is a recurring style nit. No action required before merge.

---
Issues found: 1 | Blockers: 0 | High: 0 | Medium: 0 | Nits: 1
Confidence: avg 75 | >= 90: 0 | 70-89: 1 | < 70: 0
