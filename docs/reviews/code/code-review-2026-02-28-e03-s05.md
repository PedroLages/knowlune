# Code Review: E03-S05 — Full-Text Note Search

**Date:** 2026-02-28
**Reviewer:** code-review agent (adversarial, re-run)

## What Works Well

1. Well-structured MiniSearch integration — clean separation of index lifecycle from search queries
2. `buildCourseLookup()` pre-computes maps at startup, keeping per-search cost sub-millisecond
3. Custom `filter` function on `CommandDialog` correctly bypasses cmdk filtering for note results
4. 150ms debounce with proper cleanup
5. `storeFields` configuration avoids a second Dexie lookup on every search result

## Findings

### Blockers

**B1 — Uncommitted fixes in working tree (confidence: 98)**
Six critical fixes exist ONLY as uncommitted working tree changes. Committed code has:
1. `noteSearch.ts:22` — `combineWith: 'AND'` breaks fuzzy multi-term queries
2. `noteSearch.ts:53` — Tag join/split uses spaces, corrupting multi-word tags
3. `LessonPlayer.tsx:184,195` — `[searchParams]` dependency causes over-firing effects
4. `SearchCommandPalette.tsx:144,154` — Missing highlight memoization
5. `noteSearch.test.ts` — Missing OR semantics and multi-word tag tests

Fix: Commit all working tree changes before merging. This is the third story in a row with uncommitted review fixes.

### High Priority

**H1 — Module-level singleton never disposed; silent empty results (confidence: 85)**
- `noteSearch.ts:15-24`: `searchNotesWithContext()` returns `[]` silently when index not initialized
- Fix: Add `isReady()` export or "Loading notes..." state in palette

**H2 — Empty state message misleading for non-note searches (confidence: 82)**
- `SearchCommandPalette.tsx:265-269`: "No notes found" shows even for page/course searches
- Fix: Use generic "No results found" message

**H3 — `updateInIndex` swallows all errors silently (confidence: 78)**
- `noteSearch.ts:86-93`: Empty catch block hides MiniSearch corruption errors
- Fix: Narrow catch to expected "not in index" error, log unexpected ones

**H4 — `h-4 w-4` instead of `size-4` Tailwind v4 shorthand (confidence: 75)**
- `SearchCommandPalette.tsx:279,313,334,355`: Inconsistent with codebase conventions

### Medium

**M1 — Hardcoded `bg-yellow-200` highlight (confidence: 72)**
- Won't adapt to dark mode. Fix: Use theme-aware class

**M2 — Inline `filter` function recreated every render (confidence: 70)**
- Fix: Extract to `useCallback` or module-level function

**M3 — `notesOpen` useState + useEffect redundancy (confidence: 68)**
- `LessonPlayer.tsx:63` and effect at 181-185 create subtle state flash

**M4 — E2E timing dependency on MiniSearch init (confidence: 68)**
- `story-e03-s05.spec.ts:87-93`: No explicit wait for search index readiness

### Nits

- Import inconsistency (`useMemo` missing in committed version)
- `let` module-scope maps have brief empty window during rebuild
- Arbitrary `text-[10px]` value in badge className

## Verdict

1 blocker (uncommitted fixes), 4 high, 4 medium, 3 nits.
