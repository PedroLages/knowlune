# Code Review: E11-S03 — Study Session Quality Scoring (Re-Review)

**Date:** 2026-03-15
**Reviewer:** Code Review Agent (Opus)

## Previous Findings — All Confirmed Fixed

- H1: recordInteraction Zustand direct mutation documented ✓
- H2: String interpolation → cn() in 3 components ✓
- H3: calculateQualityTrend threshold < 4 → < 2 ✓
- M7: endSession state clearing reordered ✓
- M8: Inline style ESLint comment ✓
- M9: qualityTrend uses filteredSessions ✓

## New Findings

### High Priority

**H1** — endSession persistence failure silently loses quality score (confidence: 82)
- Location: `src/stores/useSessionStore.ts:200-202`
- If `persistWithRetry` fails, `.catch()` logs but user never sees quality score dialog
- Suggestion: Dispatch `session-quality-calculated` event regardless of persistence outcome

**H2** — Empty `<SheetFooter />` and `<DialogFooter />` cause mobile padding issue (confidence: 75)
- Location: `src/app/components/session/QualityScoreDialog.tsx:77, :93`
- Continue button is inside ScoreContent, empty footer elements serve no purpose

### Medium

**M1** — SVG stroke hardcodes `var(--muted)` as string prop (confidence: 72)
- Location: `src/app/components/session/QualityScoreRing.tsx:42-65`
- Extract to constant alongside TIER_STROKE_COLORS for consistency

**M2** — `hasQualityScores` uses unfiltered sessions (confidence: 70)
- Location: `src/app/pages/SessionHistory.tsx:182`
- Shows "Stable" trend when filtered course has no scored sessions

**M3** — makeSession in E2E spec duplicates factory (confidence: 72)
- Location: `tests/e2e/story-e11-s03.spec.ts:21-47`
- Could import createStudySession and extend with display fields

### Nits

- Continue button uses `w-full` on desktop (confidence: 60)
- Progress bar h-2 has no min-height (confidence: 55)

Issues: 7 | Blockers: 0 | High: 2 | Medium: 3 | Nits: 2
