# Code Review: E11-S03 — Study Session Quality Scoring

**Date:** 2026-03-15
**Reviewer:** Code Review Agent (Opus)

## What Works Well

1. **Pure function scoring engine** (`src/lib/qualityScore.ts`): All scoring logic is side-effect-free, deterministic, and highly testable.
2. **Comprehensive unit test coverage** (`qualityScore.test.ts`): 27 test cases covering each factor function edge case, composite scoring, tier mapping, and trend calculation.
3. **Clean DB migration** (v14): New optional fields without upgrade function — backward-compatible with legacy sessions.

## Findings

### High Priority

| # | File | Confidence | Finding |
|---|------|-----------|---------|
| H1 | `src/stores/useSessionStore.ts:112` | 92 | `recordInteraction` directly mutates Zustand state (`activeSession.interactionCount = ...`) instead of `set()`. Any component subscribing to `activeSession` won't re-render on change. Document as intentional perf optimization or switch to immutable `set()`. |
| H2 | `QualityScoreRing.tsx:77`, `TrendIndicator.tsx:23`, `SessionHistory.tsx:57` | 90 | String interpolation for `className` instead of `cn()` utility. 3 instances. Doesn't merge conflicting Tailwind classes correctly. |
| H3 | `src/lib/qualityScore.ts:143-156` | 85 | `calculateQualityTrend` threshold `< 4` means 3 sessions always shows "stable" even with clear trend. Deviates from AC4 spec ("compare last 5 vs previous 5"). Consider lowering to `< 2`. |
| H4 | `tests/e2e/story-e11-s03.spec.ts` | 80 | No E2E test for QualityScoreDialog display or AC5 (real-time tracking). Primary user-facing feature untested at integration level. |

### Medium

| # | File | Confidence | Finding |
|---|------|-----------|---------|
| M1 | `src/stores/useSessionStore.ts:192-212` | 75 | `endSession` clears active state synchronously then fires event asynchronously in `.then()`. Fragile — future enhancements needing session context in dialog will fail. |
| M2 | `src/app/components/session/FactorBreakdown.tsx:80` | 72 | Inline `style={{ backgroundColor: factor.color }}` bypasses Tailwind. Acceptable for dynamic CSS variables but should have ESLint disable comment. |
| M3 | `src/app/pages/SessionHistory.tsx:173-176` | 70 | `qualityTrend` computed from all sessions, not filtered. Misleading when filtering by course. |

### Nits

| # | File | Confidence | Finding |
|---|------|-----------|---------|
| N1 | `QualityScoreDialog.tsx:29` | 65 | `rounded-[24px]` may conflict with DialogContent's `sm:rounded-lg`. Use `cn()`. |
| N2 | `src/lib/retentionMetrics.ts` | 60 | Formatting-only change unrelated to story scope. |
| N3 | `QualityScoreRing.tsx:3` | 55 | Consider co-locating tier-to-color mapping in `qualityScore.ts`. |

## Summary

Issues: 10 | Blockers: 0 | High: 4 | Medium: 3 | Nits: 3
