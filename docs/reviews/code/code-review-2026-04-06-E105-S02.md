# Code Review: E105-S02 — E2E Test Fixes and Coverage Threshold

**Date:** 2026-04-06
**Reviewer:** Claude Opus (automated)
**Story:** E105-S02
**Verdict:** PASS

## Files Reviewed

- `vite.config.ts` — testModeCspPlugin, COOP/COEP conditional, coverage threshold
- `playwright.config.ts` — PLAYWRIGHT_TEST=1 env var
- `tests/support/fixtures/local-storage-fixture.ts` — WelcomeWizard dismissal
- `tests/e2e/dashboard-reordering.spec.ts` — Section count updates
- `tests/e2e/nfr35-export.spec.ts` — Manual wizard dismissal
- `docs/known-issues.yaml` — KI-021 through KI-025, KI-029 status updates
- `docs/implementation-artifacts/sprint-status.yaml` — Epic 105 status
- `docs/implementation-artifacts/stories/E105-S02.md` — Story metadata

## Findings

### LOW

1. **Wizard dismissal duplication in nfr35-export.spec.ts** — The manual `addInitScript` for wizard/onboarding/sidebar dismissal (lines 19-29) duplicates logic already in `local-storage-fixture.ts`. Consider importing the fixture or extracting the seeding logic to a shared helper.

2. **navigateAndWait missing WelcomeWizard dismissal** — `tests/support/helpers/navigation.ts:17-25` seeds `knowlune-onboarding-v1` but not `knowlune-welcome-wizard-v1`. The story added wizard dismissal to the localStorage fixture and nfr35-export but not to the shared navigation helper. Not a regression (pre-existing tests already lacked it), but an inconsistency.

### NIT

3. **Missing blank line before testModeCspPlugin** — `vite.config.ts:450-451` has the closing brace of `serveLocalMedia()` immediately followed by the JSDoc of `testModeCspPlugin()` with no blank line separator.

## Architecture & Security

- **testModeCspPlugin** is correctly scoped to `apply: 'serve'` and gated by `PLAYWRIGHT_TEST` env var — no production impact.
- COOP/COEP conditional removal is well-documented and only affects dev server when `PLAYWRIGHT_TEST=1`.
- Coverage threshold change (70% → 55%) is documented in known-issues with follow-up plan.

## Test Quality

- Dashboard reordering tests properly updated to reflect current section counts (9 rendered, 10 in customizer).
- Slice assertion (`testIds.slice(0, manualOrder.length)`) is a good pattern for partial order validation.
- Lessons learned section is thorough and actionable.
