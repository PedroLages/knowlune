# Code Review: E04-S05 — Continue Learning Dashboard Action

**Date**: 2026-03-04
**Reviewer**: Code Review Agent (Adversarial)

## Summary

Clean component decomposition with good deleted-course handling and thorough E2E tests. Critical issue: implementation files are not committed. Several performance and accessibility concerns.

## Findings

### Blockers

**B1: Implementation files not committed (confidence: 98)**
- `ContinueLearning.tsx` is untracked, `Overview.tsx` changes unstaged
- Branch `b6ad7df` contains only docs + tests, zero functional code
- Fix: `git add src/app/components/ContinueLearning.tsx src/app/pages/Overview.tsx && git commit`

### High Priority

**H1: No `prefers-reduced-motion` guard on animations (confidence: 82)**
- `ContinueLearning.tsx:73,150,212` — `hover:scale-[1.01]` and `transition-all` lack motion guards
- Fix: Use `motion-safe:hover:scale-[1.01] motion-safe:transition-shadow`

**H2: Redundant `getAllProgress()` calls (confidence: 85)**
- Called 7+ times per Overview render (parent + child both call independently)
- Fix: Call once in Overview, pass as props, or memoize per render cycle

**H3: `getResolvedSessions()` runs on every render without memoization (confidence: 78)**
- Creates Date objects, sorts, does find() lookups on every parent re-render
- Fix: Wrap in `useMemo`

**H4: No unit tests for ContinueLearning component (confidence: 90)**
- `getResolvedSessions`, `findLessonTitle`, `getNotStartedCourses` have no unit tests
- Fix: Add unit tests for core business logic functions

### Medium

**M1: Use `size-*` shorthand (confidence: 88)**
- 12+ instances of `w-N h-N` instead of `size-N`

**M2: Hardcoded `bg-blue-100 dark:bg-blue-900/30` in 4 places (confidence: 75)**
- Not using theme tokens from `theme.css`
- Fix: Extract to semantic token or shared constant

**M3: Duplicate `getAllProgress()` between Overview and ContinueLearning (confidence: 72)**
- Both components independently call the same function
- Fix: Pass progress data as props

**M4: Dead fallback code in RecentlyAccessedRow (confidence: 70)**
- `resumeLink || ...` on line 146 is dead code since `resumeLink` is always truthy

### Nits

- No `cn()` import/usage (diverges from codebase convention)
- Lucide icons missing explicit `aria-hidden="true"`
- `getNotStartedCourses` vs `getResolvedSessions` use different inclusion criteria — worth a comment

---
Issues: 10 | Blockers: 1 | High: 3 | Medium: 4 | Nits: 3
