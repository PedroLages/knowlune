# Code Review: E109-S01 — Vocabulary Builder

**Date:** 2026-04-11
**Reviewer:** Claude Opus (automated)
**Branch:** feature/e109-s01-vocabulary-builder
**Round:** R2

## Summary

Clean, well-structured story implementing vocabulary builder with reader integration, Dexie persistence, and flashcard-style review. Good separation of concerns across components, proper design token usage, and accessible markup.

## Findings

### MEDIUM

1. **Missing error handling in `updateItem`, `advanceMastery`, `resetMastery`** — `src/stores/useVocabularyStore.ts:72-111`
   - `addItem` and `deleteItem` have try/catch with rollback, but `updateItem` (line 72), `advanceMastery` (line 92), and `resetMastery` (line 104) do optimistic updates without rollback on Dexie failure.
   - Impact: If Dexie write fails, UI shows stale state that reverts on next load.
   - Suggestion: Add try/catch with rollback pattern matching `addItem`/`deleteItem`.

### LOW

2. **`schema.test.ts` needs version/table-count update** — `src/db/__tests__/schema.test.ts`
   - The hardcoded version (37) and table list need updating for version 42 with `vocabularyItems`. Story-related test gap.

3. **Redundant `dismissOnboarding` helper** — `tests/helpers/dismiss-onboarding.ts`
   - Duplicates logic already in `navigateAndWait()`. The E2E test calls both `dismissOnboarding` in `beforeEach` AND `navigateAndWait`. Not harmful but unnecessary duplication.

4. **`EditDialog` uses custom overlay instead of shadcn Dialog** — `src/app/components/vocabulary/EditDialog.tsx`
   - Manually implements backdrop + centered card. Using `<Dialog>` from shadcn/ui would get focus trapping, Escape-to-close, and scroll-lock for free.

### NIT

5. **`new Date().toISOString()` in store** — `src/stores/useVocabularyStore.ts:73,96-97,106`
   - Production code (not test code) so this is valid, but multiple call sites could extract a `nowISO()` helper for consistency.

## Pre-Existing Issues (Not Story-Related)

- TypeScript errors in `HighlightLayer.tsx` (7 errors) — same on main
- 121 ESLint errors across codebase — none in story-changed files
- 31 unit test failures in unrelated files
- `routes.tsx` component-size warning (507 lines > 500 threshold) — pre-existing

## Verdict

**PASS** — No blockers. One MEDIUM for missing error handling consistency.
