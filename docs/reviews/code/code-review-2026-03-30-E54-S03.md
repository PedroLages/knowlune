# Code Review: E54-S03 — Completion Checkmarks (Round 2)

**Date:** 2026-03-30
**Branch:** `feature/e54-s03-completion-checkmarks-imported-course-detail`
**Reviewer:** Claude Opus 4.6 (automated)
**Round:** 2

## Diff Scope

- `src/app/components/course/LessonList.tsx` — +14 lines (StatusIndicator, deriveStatus, progressMap threading)
- `src/app/pages/ImportedCourseDetail.tsx` — +40 lines (StatusIndicator, progress card, useContentProgressStore)
- `tests/e2e/regression/e54-s03-completion-checkmarks.spec.ts` — +176 lines (4 E2E tests)
- `docs/` — story file and sprint status updates

## Architecture

### LessonList.tsx Changes (Correct)
- `deriveStatus()` helper derives `CompletionStatus` from `VideoProgress` using the 90% threshold — consistent with `CourseProgress.tsx` and the rest of the codebase.
- `progressMap` parameter added to `renderLocalGroups` — properly threaded from `LessonList` props.
- `StatusIndicator` in `mode="display"` correctly renders as non-interactive inline indicator.

### ImportedCourseDetail.tsx Changes (Dead Code)
- `ImportedCourseDetail` is not referenced in `routes.tsx` — replaced by `UnifiedCourseDetail` (line 4 of UnifiedCourseDetail.tsx documents this).
- All 40 lines of changes apply to a component that is never rendered.
- This is technical debt, not a bug — the file should be deleted in a cleanup story.

### Consistency Check
- `LessonList.deriveStatus()` threshold (>=90%) matches `UnifiedCourseDetail` completed filter (line 159: `completionPercentage >= 90`).
- `LessonList` receives `Map<string, VideoProgress>` keyed by `video.id` — matches `UnifiedCourseDetail`'s `progressMap` shape.

## Findings

| Severity | Finding | Location | Confidence |
|----------|---------|----------|------------|
| HIGH | **Broken unit tests**: 2 unit tests in `ImportedCourseDetail.test.tsx` fail because `StatusIndicator` renders before the title text, changing `toHaveTextContent()` assertions. Must fix before merge. | `src/app/pages/__tests__/ImportedCourseDetail.test.tsx` | 95 |
| MEDIUM | **Dead code**: `ImportedCourseDetail.tsx` is not routed. All 40 lines of changes are unreachable. Consider removing the file or deferring changes to a cleanup story. | `src/app/pages/ImportedCourseDetail.tsx` | 99 |
| LOW | **Prettier formatting**: 5 files auto-formatted by Prettier (4 pre-existing, 1 branch-related). Need to commit formatting fixes. | Multiple files | 90 |

## Verdict

1 HIGH finding (broken unit tests) must be addressed. The feature works correctly through the live path (`LessonList.tsx` + `UnifiedCourseDetail`), but the existing unit tests for `ImportedCourseDetail` are broken by the component changes.
