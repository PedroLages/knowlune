# Code Review: E91-S02 Local Course Visual Parity

**Date:** 2026-03-30
**Reviewer:** Claude Opus 4.6 (automated)
**Story:** E91-S02 — Local Course Visual Parity (Progress Bars + Thumbnails)

## Files Changed

- `src/app/components/course/LessonList.tsx` — Replaced StatusIndicator dots with progress bars, thumbnails, completion badges in local video items; added thumbnail placeholders to PDF items
- `tests/e2e/story-e91-s02.spec.ts` — E2E tests for all 5 acceptance criteria

## Findings

### STORY-RELATED

No blockers or high-severity issues found.

**MEDIUM: No issues found.**

**LOW:**

1. **LOW: Completed videos show full progress bar (visual noise)** — `LessonList.tsx:442`
   - When `percent >= 90` (completed), the progress bar still renders. The YouTube renderer does the same, so this is parity-correct but arguably redundant with the checkmark badge.
   - Verdict: Consistent with YouTube behavior. Not a defect.

**NITS:**

1. **NIT: `isCompleted` threshold is 90%, not 100%** — `LessonList.tsx:380`
   - Both local and YouTube renderers use `percent >= 90` as the completion threshold. This is consistent but undocumented. A named constant would improve readability.

### PRE-EXISTING

1. **WARNING: 3 ESLint parse errors in non-story files** — Binary/non-JS files being parsed
2. **WARNING: 5 TypeScript errors in `src/db/__tests__/schema.test.ts`** — `CardState` type mismatch (pre-existing)
3. **WARNING: 24 ESLint warnings across codebase** — Unused vars, inline styles, silent catches (all pre-existing files)

## Architecture Assessment

- Clean removal of `StatusIndicator` + `deriveStatus` — no orphaned references
- Local video rendering now mirrors YouTube rendering structure (index circle, thumbnail, info block with progress)
- PDF items also received thumbnail placeholder treatment for visual consistency
- No code duplication introduced — both renderers share the same design pattern (though not extracted into a shared component)

## Verdict: PASS
