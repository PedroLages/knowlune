# Code Review: E21-S05 — User Engagement Preference Controls

**Date:** 2026-03-24
**Reviewer:** Claude Opus 4.6 (automated)
**Story:** E21-S05 — User Engagement Preference Controls
**Branch:** feature/e21-s05-user-engagement-preference-controls
**Round:** 2 (re-review after fixes)

## Summary

Clean implementation of engagement preference controls using Zustand store with localStorage persistence. The `useEngagementVisible` hook provides optimal re-render performance via Zustand selectors. Feature gating is correctly applied to AchievementBanner, StudyStreakCalendar, StreakMilestoneToast, ChallengeMilestoneToast, CompletionModal confetti, MomentumBadge, and Framer Motion animations.

All 6 findings from Round 1 have been addressed in commit `74d0bf5a`.

## Round 1 Findings — Verification

| # | Severity | Finding | Status |
|---|----------|---------|--------|
| 1 | MEDIUM | Empty div placeholder causes visual gap when streaks disabled | FIXED — Grid layout now conditional (`lg:grid-cols-[3fr_2fr]` only when streaks shown) |
| 2 | MEDIUM | Lessons learned section is placeholder text | FIXED — Meaningful lessons populated |
| 3 | LOW | E2E test uses `new Date().toISOString()` in addInitScript | FIXED — Replaced with fixed ISO string constant |
| 4 | LOW | `celebrateCompletion()` standalone function not gated | FIXED — Now uses `useEngagementPrefsStore.getState().animations` |
| 5 | HIGH* | Missing E2E tests for badges toggle visibility | FIXED — Added badge toggle test |
| 6 | HIGH* | Missing E2E tests for animations toggle | FIXED — Added animations persistence tests |

*From testing review report.

## Round 2 Findings

### STORY-RELATED

#### LOW: E2E badge test asserts localStorage after `addInitScript` re-run
- **File:** `tests/e2e/regression/story-e21-s05.spec.ts:145-185`
- **Issue:** The badge toggle test navigated to Overview after clicking the toggle, but `addInitScript` re-ran on navigation, overwriting the localStorage value the test was about to assert. This caused a consistent test failure (not flaky).
- **Fix applied:** Moved the localStorage assertion to before the navigation. The test now verifies persistence on the Settings page immediately after toggling.
- **Status:** FIXED in this review round.

#### NIT: ProgressStats grid-cols-4 shows 3 items when streaks hidden
- **File:** `src/app/components/ProgressStats.tsx:28`
- **Issue:** The stats grid uses `lg:grid-cols-4` but renders only 3 `StatsCard` children when streaks are toggled OFF, leaving an empty 4th column cell. Cosmetic only; the cards fill their cells and the gap is not visually prominent.
- **Suggestion:** Could make the grid responsive to item count (e.g., `lg:grid-cols-${showStreaks ? 4 : 3}`) but this is very low priority.

### PRE-EXISTING

#### WARNING: Silent catch in Settings.tsx (line 253)
- Pre-existing ESLint warning. Not introduced by this story.

#### WARNING: Lint warnings in scripts/ and server/ files
- 85+ warnings total, all in files untouched by this story. Pre-existing tech debt.

#### WARNING: Prettier formatting issues in 7 files
- None are story-changed files. Pre-existing.

## Architecture Assessment

**Store Design:** Clean Zustand store pattern with typed `setPreference` generic method. Defensive `loadPersistedPrefs` with type guards against corrupted localStorage data. Good.

**Hook Design:** `useEngagementVisible` uses Zustand selectors correctly, ensuring components only re-render when their specific feature toggle changes.

**MotionConfig Integration:** Using `MotionConfig reducedMotion={showAnimations ? 'user' : 'always'}` at the root of Overview is elegant — it respects both user OS preferences and the in-app toggle.

**Component Gating:** Consistent pattern across all gated components — either conditional rendering (`{showX && <Component />}`) or early return (`if (!badgesEnabled) return null`).

**`celebrateCompletion()` imperative gating:** Using `useEngagementPrefsStore.getState()` for the standalone function is the correct Zustand pattern for imperative access outside React components.

## Quality Gates

| Gate | Result |
|------|--------|
| Build | PASS |
| Lint | PASS (0 errors; warnings are pre-existing) |
| TypeScript | PASS |
| Prettier | PASS (issues in non-story files only) |
| Unit Tests | PASS (2206/2206) |
| E2E Tests | PASS (12/12, 3x burn-in: 36/36) |

## Verdict

**PASS.** All Round 1 findings verified fixed. One new LOW finding (badge test `addInitScript` race) was found and fixed during this review. One NIT remains (ProgressStats grid-cols-4 with 3 items). No blockers or high-severity issues remain.
