# Code Review: E59-S04 — Zustand Store FSRS Migration

**Date:** 2026-03-30
**Story:** E59-S04 — Zustand Store Updates (Flashcard and Review Stores)
**Branch:** `feature/e59-s04-zustand-store-updates`
**Reviewer:** Claude Opus 4.6 (automated)

## Files Changed

- `src/stores/useFlashcardStore.ts`
- `src/stores/useReviewStore.ts`
- `src/app/components/figma/FlashcardReviewCard.tsx`
- `src/app/components/figma/ReviewCard.tsx`
- `src/app/pages/Flashcards.tsx`
- `src/app/pages/ReviewQueue.tsx`
- `src/lib/exportService.ts`
- `src/lib/retentionMetrics.ts`
- `src/services/NotificationService.ts`
- `src/stores/__tests__/useFlashcardStore.test.ts`
- `src/stores/__tests__/useReviewStore.test.ts`

## Summary

Clean SM-2 to FSRS migration across both Zustand stores and all consumer files. The migration follows the single-gateway pattern established in E59-S03 (`spacedRepetition.ts`), with stores delegating to `calculateNextReview`, `predictRetention`, and `isDue`. All FSRS scheduling fields (stability, difficulty, reps, lapses, state, elapsed_days, scheduled_days, due, last_review) are propagated correctly through create, rate, and query paths.

## Findings

### STORY-RELATED

#### MEDIUM

1. **Flashcards.tsx catch block lacks toast feedback** (line 120) — The `handleLoadFlashcards` catch block sets `loadError` state and renders an error UI, but the ESLint `no-silent-catch` rule flags it because it doesn't call `toast.error()`. While the error IS surfaced to the user via the rendered error state, adding a comment `// silent-catch-ok — error surfaced via loadError UI state` would suppress the warning and document the intent.

#### LOW

2. **Formatting inconsistency** (fixed) — Three story files had Prettier formatting issues. Committed fix in `5b1ad075`.

### PRE-EXISTING

3. **TypeScript errors in `spacedRepetition.test.ts`** (26 errors) — This test file uses old SM-2 types (`interval`, `easeFactor`, `nextReviewAt`, `reviewedAt`) that no longer exist after FSRS migration. The story explicitly defers this to E59-S07 (unit test story). These are expected and documented in the story's Implementation Notes.

4. **Unit test coverage threshold** — Coverage for lines (69.35%) does not meet global threshold (70%). This is pre-existing and not caused by this story.

5. **Lint warnings in non-story files** — 24 of 25 lint warnings are in files not changed by this story (`UnifiedLessonPlayer.tsx`, `useHasQuiz.ts`, several E2E test files).

6. **Prettier warnings in non-story files** — 6 of 9 formatting issues are in files not changed by this story (CourseHeader, CourseProgress, LocalVideoContent, PlayerSidePanel, YouTubeVideoContent, UnifiedLessonPlayer).

## Architecture Assessment

- **Single-gateway pattern**: Correctly maintained. Only `spacedRepetition.ts` imports from `ts-fsrs`; stores and consumers use the wrapper functions.
- **Optimistic updates with rollback**: Both stores correctly implement optimistic update pattern with rollback on persistence failure.
- **Type safety**: `ReviewRecord` and `Flashcard` types are structurally compatible with `FSRSCardState` (the parameter type of `calculateNextReview`), so no type assertions needed.
- **Null-card handling**: Both stores correctly pass `null` for first-time reviews (useFlashcardStore checks `reps > 0`, useReviewStore checks `existing ?? null`).

## Verdict

**PASS** — No blockers. One medium issue (lint warning suppression) is cosmetic.
