# Code Review: E43-S08 Auth UX Polish

**Date:** 2026-03-28
**Reviewer:** Claude Opus 4.6 (automated)
**Story:** E43-S08 — Auth UX Polish
**Branch:** feature/e43-s08-auth-ux-polish

## Summary

Auth UX polish story: OAuth state fix, Google avatar hydration, login navigation, field rename. Well-structured changes with good security practices (HTTPS-only avatar URLs, CSP updates, referrerPolicy). Two issues found requiring fixes.

## STORY-RELATED Issues

### HIGH: useAuthLifecycle test suite broken — missing `getSession` mock (6 failures)

**File:** `src/app/hooks/__tests__/useAuthLifecycle.test.ts`
**Lines:** 13-22

The story added `supabase.auth.getSession()` to `useAuthLifecycle.ts:62` but did not update the test mock to include `getSession`. All 6 tests fail with `TypeError: supabase.auth.getSession is not a function`.

**Fix:** Add `getSession: vi.fn().mockResolvedValue({ data: { session: null } })` to the mock at line 19.

### MEDIUM: Invalid ESLint disable directive in Login.tsx

**File:** `src/app/pages/Login.tsx`
**Line:** 37

`// eslint-disable-next-line react-hooks/exhaustive-deps` references a rule that doesn't exist in the project's ESLint config (the project uses `react-hooks-async`, not `react-hooks`). This causes an ESLint error.

**Fix:** Remove the disable comment. The dependency array `[initialized, user, navigate]` is correct and complete for this effect (the omitted `navigateToReturnRoute` is a stable function defined in the component scope that only depends on `navigate`). Alternatively, include `navigateToReturnRoute` in the deps array since it's recreated each render.

### LOW: Prettier formatting issues in story-changed files

**Files:** `src/lib/settings.ts`, `src/lib/__tests__/settings.test.ts`

Both files fail Prettier check.

**Fix:** Run `npx prettier --write src/lib/settings.ts src/lib/__tests__/settings.test.ts`

## PRE-EXISTING Issues (not in story scope)

- TypeScript errors in `src/services/__tests__/NotificationService.test.ts` (3 errors)
- Unit test failures in `src/lib/entitlement/__tests__/isPremium.test.ts` (9 failures)
- Unit test failure in `src/app/pages/__tests__/Courses.test.tsx` (1 failure)
- ESLint warnings in Layout.tsx catch blocks (lines 275, 307) — `no-silent-catch`
- Multiple ESLint warnings in test files (unused vars, unused eslint-disable directives)

## Positive Observations

1. **Security**: HTTPS-only protocol guard on avatar URLs prevents XSS via `javascript:` or `data:` URL injection
2. **CSP updates**: Both `index.html` and `vite.config.ts` updated (dual CSP awareness)
3. **Radix Avatar pattern**: Correct sibling pattern (AvatarImage + AvatarFallback) replaces broken ternary
4. **referrerPolicy**: `no-referrer` on AvatarImage prevents Google CDN 403
5. **Race condition handling**: `ignore` flag + subscription-before-getSession ordering follows Supabase best practices
6. **Test coverage**: 13 new unit tests for Google OAuth metadata hydration with good edge case coverage
7. **Hash cleanup**: Cosmetic but user-facing improvement removing `#access_token` from URL
