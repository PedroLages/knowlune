# Code Review: E22-S02 Model Auto-Discovery (Round 2)

**Date:** 2026-03-25
**Story:** E22-S02 — Model Auto-Discovery
**Reviewer:** Claude Opus 4.6 (automated)
**Context:** Round 2 — verifying fixes from Round 1 (4 issues: 2 MEDIUM, 2 LOW)

## Round 1 Fix Verification

| # | Severity | Issue | Status |
|---|----------|-------|--------|
| 1 | MEDIUM | Missing async cleanup in useEffect | FIXED — `cancelled` flag with cleanup function at OllamaModelPicker.tsx:87-121 |
| 2 | MEDIUM | Prettier formatting not applied | FIXED — `npx prettier --check` passes clean |
| 3 | LOW | `onModelSelect` callback instability | FIXED — wrapped in `useCallback` at AIConfigurationSettings.tsx:282-290 |
| 4 | LOW | `formatModelSize` no negative guard | FIXED — `if (bytes <= 0) return '0 B'` at aiConfiguration.ts:386 |

**All 4 Round 1 issues are resolved.**

## Round 2 New Findings

### STORY-RELATED

#### LOW

1. **Duplicated fetch logic between callback and useEffect** — `OllamaModelPicker.tsx:60-83` (fetchModels callback) and `OllamaModelPicker.tsx:86-122` (useEffect) contain identical fetch/parse/error logic. The useEffect inlines the fetch rather than calling `fetchModels()`. This means a bug fix in one path must be manually duplicated in the other. Low risk since both paths are small and stable, but could be DRYed by having the useEffect call `fetchModels()` directly.

### PRE-EXISTING

2. **E2E test failures** — 5 tests fail in `onboarding.spec.ts` (4) and `overview.spec.ts` (1). Verified these fail identically on `main`. Not introduced by this story.

3. **ESLint warnings** — 166 warnings across the codebase (test anti-patterns, `any` types). None in story-changed files. Pre-existing.

## Quality Gates Summary

| Gate | Result |
|------|--------|
| Build | PASS |
| Lint | PASS (0 errors, 166 pre-existing warnings) |
| Type check | PASS (0 errors) |
| Prettier | PASS |
| Unit tests | PASS (147 files, 2403 tests) |
| E2E smoke | 4 passed, 5 failed (all pre-existing) |

## Architecture Assessment

No changes since Round 1. Architecture is clean:
- SSRF protection on proxy endpoint via `isAllowedOllamaUrl()`
- Static `fetchModels()` for UI discovery without full client instance
- Proper error taxonomy (network, timeout, HTTP)
- Cancelled-fetch cleanup pattern in useEffect

## Verdict

PASS. All Round 1 issues fixed. One new LOW finding (duplicated fetch logic) is a minor code smell, not a bug. No blockers or high-severity issues.
