# Cloudflare Pages SPA fallback.
# Without this, direct navigation to client-side routes (e.g. /courses) 404s
# because the static host tries to resolve them as files before React Router
# can handle them. Missing /assets/* requests are intercepted by the Pages
# Function in functions/assets/[[path]].ts before this fallback is evaluated.
/*    /index.html   200
