# Asset 404 handling is managed by the Cloudflare Pages Function at
# /functions/assets/[[path]].ts. It uses env.ASSETS.fetch() to distinguish
# existing files from truly missing ones, and returns a proper 404 with
# text/plain content type for missing assets — never the SPA fallback HTML.
#
# The unsupported 404 status rules previously here were silently ignored by
# Cloudflare Pages (only 200/301/302/303/307/308 are valid redirect statuses).

# Cloudflare Pages SPA fallback.
# Direct navigation to client-side routes (e.g. /courses) is resolved by
# serving /index.html with a 200 status. Using 301/302 breaks deep links.
# The /functions/assets/[[path]].ts Function handles asset requests before
# this rule is reached, so only genuine SPA routes get the fallback.
/*    /index.html   200
