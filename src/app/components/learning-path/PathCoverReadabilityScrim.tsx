/** Shared bottom-weighted gradient over path cover photos (card strip + hero). */
export function PathCoverReadabilityScrim() {
  return (
    <div
      className="absolute inset-0 bg-gradient-to-b from-black/10 to-black/50"
      aria-hidden
    />
  )
}
